/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  IncomeVerificationCreateRequest,
  incomeVerificationCreateRequestSchema,
} from '../models/incomeVerificationCreateRequest.js';
import {
  IncomeVerificationCreateResponse,
  incomeVerificationCreateResponseSchema,
} from '../models/incomeVerificationCreateResponse.js';
import {
  IncomeVerificationDocumentsDownloadRequest,
  incomeVerificationDocumentsDownloadRequestSchema,
} from '../models/incomeVerificationDocumentsDownloadRequest.js';
import {
  IncomeVerificationPaystubGetRequest,
  incomeVerificationPaystubGetRequestSchema,
} from '../models/incomeVerificationPaystubGetRequest.js';
import {
  IncomeVerificationPaystubGetResponse,
  incomeVerificationPaystubGetResponseSchema,
} from '../models/incomeVerificationPaystubGetResponse.js';
import {
  IncomeVerificationPaystubsGetRequest,
  incomeVerificationPaystubsGetRequestSchema,
} from '../models/incomeVerificationPaystubsGetRequest.js';
import {
  IncomeVerificationPaystubsGetResponse,
  incomeVerificationPaystubsGetResponseSchema,
} from '../models/incomeVerificationPaystubsGetResponse.js';
import {
  IncomeVerificationPrecheckRequest,
  incomeVerificationPrecheckRequestSchema,
} from '../models/incomeVerificationPrecheckRequest.js';
import {
  IncomeVerificationPrecheckResponse,
  incomeVerificationPrecheckResponseSchema,
} from '../models/incomeVerificationPrecheckResponse.js';
import {
  IncomeVerificationRefreshRequest,
  incomeVerificationRefreshRequestSchema,
} from '../models/incomeVerificationRefreshRequest.js';
import {
  IncomeVerificationRefreshResponse,
  incomeVerificationRefreshResponseSchema,
} from '../models/incomeVerificationRefreshResponse.js';
import {
  IncomeVerificationSummaryGetRequest,
  incomeVerificationSummaryGetRequestSchema,
} from '../models/incomeVerificationSummaryGetRequest.js';
import {
  IncomeVerificationSummaryGetResponse,
  incomeVerificationSummaryGetResponseSchema,
} from '../models/incomeVerificationSummaryGetResponse.js';
import {
  IncomeVerificationTaxformsGetRequest,
  incomeVerificationTaxformsGetRequestSchema,
} from '../models/incomeVerificationTaxformsGetRequest.js';
import {
  IncomeVerificationTaxformsGetResponse,
  incomeVerificationTaxformsGetResponseSchema,
} from '../models/incomeVerificationTaxformsGetResponse.js';
import { unknown } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorError } from '../errors/errorError.js';

export class IncomeApi extends BaseApi {
  /**
   * `/income/verification/taxforms/get` returns the information collected from taxforms that were used
   * to verify an end user's. It can be called once the status of the verification has been set to
   * `VERIFICATION_STATUS_PROCESSING_COMPLETE`, as reported by the `INCOME: verification_status` webhook.
   * Attempting to call the endpoint before verification has been completed will result in an error.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationTaxformsGet(
    body: IncomeVerificationTaxformsGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationTaxformsGetResponse>> {
    const req = this.createRequest('POST', '/income/verification/taxforms/get');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationTaxformsGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationTaxformsGetResponseSchema,
      requestOptions
    );
  }

  /**
   * (Deprecated) Retrieve information from a single paystub used for income verification
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationPaystubGet(
    body: IncomeVerificationPaystubGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationPaystubGetResponse>> {
    const req = this.createRequest('POST', '/income/verification/paystub/get');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationPaystubGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationPaystubGetResponseSchema,
      requestOptions
    );
  }

  /**
   * `/income/verification/documents/download` provides the ability to download the source paystub PDF
   * that the end user uploaded via Paystub Import.
   *
   * The response to `/income/verification/documents/download` is a ZIP file in binary data. The
   * `request_id`  is returned in the `Plaid-Request-ID` header.
   *
   * For Payroll Income, the most recent file available for download with the payroll provider will also
   * be available from this endpoint.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationDocumentsDownload(
    body: IncomeVerificationDocumentsDownloadRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown>> {
    const req = this.createRequest(
      'POST',
      '/income/verification/documents/download'
    );
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationDocumentsDownloadRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(unknown(), requestOptions);
  }

  /**
   * `/income/verification/refresh` refreshes a given income verification.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationRefresh(
    body: IncomeVerificationRefreshRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationRefreshResponse>> {
    const req = this.createRequest('POST', '/income/verification/refresh');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationRefreshRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationRefreshResponseSchema,
      requestOptions
    );
  }

  /**
   * `/income/verification/paystubs/get` returns the information collected from the paystubs that were
   * used to verify an end user's income. It can be called once the status of the verification has been
   * set to `VERIFICATION_STATUS_PROCESSING_COMPLETE`, as reported by the `INCOME: verification_status`
   * webhook. Attempting to call the endpoint before verification has been completed will result in an
   * error.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationPaystubsGet(
    body: IncomeVerificationPaystubsGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationPaystubsGetResponse>> {
    const req = this.createRequest('POST', '/income/verification/paystubs/get');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationPaystubsGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationPaystubsGetResponseSchema,
      requestOptions
    );
  }

  /**
   * `/income/verification/precheck` returns whether a given user is supportable by the income product
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationPrecheck(
    body: IncomeVerificationPrecheckRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationPrecheckResponse>> {
    const req = this.createRequest('POST', '/income/verification/precheck');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationPrecheckRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationPrecheckResponseSchema,
      requestOptions
    );
  }

  /**
   * `/income/verification/summary/get` returns a verification summary for the income that was verified
   * for an end user. It can be called once the status of the verification has been set to
   * `VERIFICATION_STATUS_PROCESSING_COMPLETE`, as reported by the `INCOME: verification_status` webhook.
   * Attempting to call the endpoint before verification has been completed will result in an error.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationSummaryGet(
    body: IncomeVerificationSummaryGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationSummaryGetResponse>> {
    const req = this.createRequest('POST', '/income/verification/summary/get');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationSummaryGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationSummaryGetResponseSchema,
      requestOptions
    );
  }

  /**
   * `/income/verification/create` begins the income verification process by returning an
   * `income_verification_id`. You can then provide the `income_verification_id` to `/link/token/create`
   * under the `income_verification` parameter in order to create a Link instance that will prompt the
   * user to go through the income verification flow. Plaid will fire an `INCOME` webhook once the user
   * completes the Payroll Income flow, or when the uploaded documents in the Document Income flow have
   * finished processing.
   *
   * @param body
   * @return Response from the API call
   */
  async incomeVerificationCreate(
    body: IncomeVerificationCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IncomeVerificationCreateResponse>> {
    const req = this.createRequest('POST', '/income/verification/create');
    const mapped = req.prepareArgs({
      body: [body, incomeVerificationCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      incomeVerificationCreateResponseSchema,
      requestOptions
    );
  }
}
