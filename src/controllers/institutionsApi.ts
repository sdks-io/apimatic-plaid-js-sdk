/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  InstitutionsGetByIdRequest,
  institutionsGetByIdRequestSchema,
} from '../models/institutionsGetByIdRequest.js';
import {
  InstitutionsGetByIdResponse,
  institutionsGetByIdResponseSchema,
} from '../models/institutionsGetByIdResponse.js';
import {
  InstitutionsGetRequest,
  institutionsGetRequestSchema,
} from '../models/institutionsGetRequest.js';
import {
  InstitutionsGetResponse,
  institutionsGetResponseSchema,
} from '../models/institutionsGetResponse.js';
import {
  InstitutionsSearchRequest,
  institutionsSearchRequestSchema,
} from '../models/institutionsSearchRequest.js';
import {
  InstitutionsSearchResponse,
  institutionsSearchResponseSchema,
} from '../models/institutionsSearchResponse.js';
import { BaseApi } from './baseApi.js';
import { ErrorError } from '../errors/errorError.js';

export class InstitutionsApi extends BaseApi {
  /**
   * Returns a JSON response containing details on a specified financial institution currently supported
   * by Plaid.
   *
   * @param body
   * @return Response from the API call
   */
  async institutionsGetById(
    body: InstitutionsGetByIdRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionsGetByIdResponse>> {
    const req = this.createRequest('POST', '/institutions/get_by_id');
    const mapped = req.prepareArgs({
      body: [body, institutionsGetByIdRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(institutionsGetByIdResponseSchema, requestOptions);
  }

  /**
   * Returns a JSON response containing details on all financial institutions currently supported by
   * Plaid. Because Plaid supports thousands of institutions, results are paginated.
   *
   * If there is no overlap between an institution’s enabled products and a client’s enabled products,
   * then the institution will be filtered out from the response. As a result, the number of institutions
   * returned may not match the count specified in the call.
   *
   * @param body
   * @return Response from the API call
   */
  async institutionsGet(
    body: InstitutionsGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionsGetResponse>> {
    const req = this.createRequest('POST', '/institutions/get');
    const mapped = req.prepareArgs({
      body: [body, institutionsGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(institutionsGetResponseSchema, requestOptions);
  }

  /**
   * Returns a JSON response containing details for institutions that match the query parameters, up to a
   * maximum of ten institutions per query.
   *
   * @param body
   * @return Response from the API call
   */
  async institutionsSearch(
    body: InstitutionsSearchRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<InstitutionsSearchResponse>> {
    const req = this.createRequest('POST', '/institutions/search');
    const mapped = req.prepareArgs({
      body: [body, institutionsSearchRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(institutionsSearchResponseSchema, requestOptions);
  }
}
