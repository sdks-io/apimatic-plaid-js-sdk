/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  ProcessorApexProcessorTokenCreateRequest,
  processorApexProcessorTokenCreateRequestSchema,
} from '../models/processorApexProcessorTokenCreateRequest.js';
import {
  ProcessorAuthGetRequest,
  processorAuthGetRequestSchema,
} from '../models/processorAuthGetRequest.js';
import {
  ProcessorAuthGetResponse,
  processorAuthGetResponseSchema,
} from '../models/processorAuthGetResponse.js';
import {
  ProcessorBalanceGetRequest,
  processorBalanceGetRequestSchema,
} from '../models/processorBalanceGetRequest.js';
import {
  ProcessorBalanceGetResponse,
  processorBalanceGetResponseSchema,
} from '../models/processorBalanceGetResponse.js';
import {
  ProcessorBankTransferCreateRequest,
  processorBankTransferCreateRequestSchema,
} from '../models/processorBankTransferCreateRequest.js';
import {
  ProcessorBankTransferCreateResponse,
  processorBankTransferCreateResponseSchema,
} from '../models/processorBankTransferCreateResponse.js';
import {
  ProcessorIdentityGetRequest,
  processorIdentityGetRequestSchema,
} from '../models/processorIdentityGetRequest.js';
import {
  ProcessorIdentityGetResponse,
  processorIdentityGetResponseSchema,
} from '../models/processorIdentityGetResponse.js';
import {
  ProcessorStripeBankAccountTokenCreateRequest,
  processorStripeBankAccountTokenCreateRequestSchema,
} from '../models/processorStripeBankAccountTokenCreateRequest.js';
import {
  ProcessorStripeBankAccountTokenCreateResponse,
  processorStripeBankAccountTokenCreateResponseSchema,
} from '../models/processorStripeBankAccountTokenCreateResponse.js';
import {
  ProcessorTokenCreateRequest,
  processorTokenCreateRequestSchema,
} from '../models/processorTokenCreateRequest.js';
import {
  ProcessorTokenCreateResponse,
  processorTokenCreateResponseSchema,
} from '../models/processorTokenCreateResponse.js';
import { BaseApi } from './baseApi.js';
import { ErrorError } from '../errors/errorError.js';

export class ProcessorApi extends BaseApi {
  /**
   * Use the `/processor/bank_transfer/create` endpoint to initiate a new bank transfer as a processor
   *
   * @param body
   * @return Response from the API call
   */
  async processorBankTransferCreate(
    body: ProcessorBankTransferCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorBankTransferCreateResponse>> {
    const req = this.createRequest('POST', '/processor/bank_transfer/create');
    const mapped = req.prepareArgs({
      body: [body, processorBankTransferCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      processorBankTransferCreateResponseSchema,
      requestOptions
    );
  }

  /**
   * The `/processor/balance/get` endpoint returns the real-time balance for each of an Item's accounts.
   * While other endpoints may return a balance object, only `/processor/balance/get` forces the
   * available and current balance fields to be refreshed rather than cached.
   *
   * @param body         The `/processor/balance/get` endpoint returns the real-
   *                                                          time balance for the account associated with a given
   *                                                          `processor_token`.  The current balance is the total
   *                                                          amount of funds in the account. The available balance is
   *                                                          the current balance less any outstanding holds or debits
   *                                                          that have not yet posted to the account.  Note that not
   *                                                          all institutions calculate the available balance. In the
   *                                                          event that available balance is unavailable from the
   *                                                          institution, Plaid will return an available balance value
   *                                                          of `null`.
   * @return Response from the API call
   */
  async processorBalanceGet(
    body: ProcessorBalanceGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorBalanceGetResponse>> {
    const req = this.createRequest('POST', '/processor/balance/get');
    const mapped = req.prepareArgs({
      body: [body, processorBalanceGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(processorBalanceGetResponseSchema, requestOptions);
  }

  /**
   * The `/processor/auth/get` endpoint returns the bank account and bank identification number (such as
   * the routing number, for US accounts), for a checking or savings account that's associated with a
   * given `processor_token`. The endpoint also returns high-level account data and balances when
   * available.
   *
   * @param body
   * @return Response from the API call
   */
  async processorAuthGet(
    body: ProcessorAuthGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorAuthGetResponse>> {
    const req = this.createRequest('POST', '/processor/auth/get');
    const mapped = req.prepareArgs({
      body: [body, processorAuthGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(processorAuthGetResponseSchema, requestOptions);
  }

  /**
   * The `/processor/identity/get` endpoint allows you to retrieve various account holder information on
   * file with the financial institution, including names, emails, phone numbers, and addresses.
   *
   * @param body
   * @return Response from the API call
   */
  async processorIdentityGet(
    body: ProcessorIdentityGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorIdentityGetResponse>> {
    const req = this.createRequest('POST', '/processor/identity/get');
    const mapped = req.prepareArgs({
      body: [body, processorIdentityGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(processorIdentityGetResponseSchema, requestOptions);
  }

  /**
   * Used to create a token suitable for sending to Apex to enable Plaid-Apex integrations.
   *
   * @param body
   * @return Response from the API call
   */
  async processorApexProcessorTokenCreate(
    body: ProcessorApexProcessorTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorTokenCreateResponse>> {
    const req = this.createRequest(
      'POST',
      '/processor/apex/processor_token/create'
    );
    const mapped = req.prepareArgs({
      body: [body, processorApexProcessorTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(processorTokenCreateResponseSchema, requestOptions);
  }

  /**
   * Used to create a token suitable for sending to one of Plaid's partners to enable integrations. Note
   * that Stripe partnerships use bank account tokens instead; see
   * `/processor/stripe/bank_account_token/create` for creating tokens for use with Stripe integrations.
   *
   * @param body
   * @return Response from the API call
   */
  async processorTokenCreate(
    body: ProcessorTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorTokenCreateResponse>> {
    const req = this.createRequest('POST', '/processor/token/create');
    const mapped = req.prepareArgs({
      body: [body, processorTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(processorTokenCreateResponseSchema, requestOptions);
  }

  /**
   * Used to create a token suitable for sending to Stripe to enable Plaid-Stripe integrations. For a
   * detailed guide on integrating Stripe, see [Add Stripe to your app](https://plaid.
   * com/docs/auth/partnerships/stripe/).
   *
   * @param body
   * @return Response from the API call
   */
  async processorStripeBankAccountTokenCreate(
    body: ProcessorStripeBankAccountTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<ProcessorStripeBankAccountTokenCreateResponse>> {
    const req = this.createRequest(
      'POST',
      '/processor/stripe/bank_account_token/create'
    );
    const mapped = req.prepareArgs({
      body: [body, processorStripeBankAccountTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      processorStripeBankAccountTokenCreateResponseSchema,
      requestOptions
    );
  }
}
