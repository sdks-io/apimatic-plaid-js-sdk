/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  LinkTokenCreateRequest,
  linkTokenCreateRequestSchema,
} from '../models/linkTokenCreateRequest.js';
import {
  LinkTokenCreateResponse,
  linkTokenCreateResponseSchema,
} from '../models/linkTokenCreateResponse.js';
import {
  LinkTokenGetRequest,
  linkTokenGetRequestSchema,
} from '../models/linkTokenGetRequest.js';
import {
  LinkTokenGetResponse,
  linkTokenGetResponseSchema,
} from '../models/linkTokenGetResponse.js';
import { BaseApi } from './baseApi.js';

export class LinkApi extends BaseApi {
  /**
   * The `/link/token/get` endpoint gets information about a previously-created `link_token` using the
   * `/link/token/create` endpoint. It can be useful for debugging purposes.
   *
   * @param body
   * @return Response from the API call
   */
  async linkTokenGet(
    body: LinkTokenGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkTokenGetResponse>> {
    const req = this.createRequest('POST', '/link/token/get');
    const mapped = req.prepareArgs({ body: [body, linkTokenGetRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(linkTokenGetResponseSchema, requestOptions);
  }

  /**
   * The `/link/token/create` endpoint creates a `link_token`, which is required as a parameter when
   * initializing Link. Once Link has been initialized, it returns a `public_token`, which can then be
   * exchanged for an `access_token` via `/item/public_token/exchange` as part of the main Link flow.
   *
   * A `link_token` generated by `/link/token/create` is also used to initialize other Link flows, such
   * as the update mode flow for tokens with expired credentials, or the Payment Initiation (Europe) flow.
   *
   * @param body
   * @return Response from the API call
   */
  async linkTokenCreate(
    body: LinkTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<LinkTokenCreateResponse>> {
    const req = this.createRequest('POST', '/link/token/create');
    const mapped = req.prepareArgs({
      body: [body, linkTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(linkTokenCreateResponseSchema, requestOptions);
  }
}
