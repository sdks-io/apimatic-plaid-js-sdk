/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  IdentityGetRequest,
  identityGetRequestSchema,
} from '../models/identityGetRequest.js';
import {
  IdentityGetResponse,
  identityGetResponseSchema,
} from '../models/identityGetResponse.js';
import { BaseApi } from './baseApi.js';

export class IdentityApi extends BaseApi {
  /**
   * The `/identity/get` endpoint allows you to retrieve various account holder information on file with
   * the financial institution, including names, emails, phone numbers, and addresses. Only name data is
   * guaranteed to be returned; other fields will be empty arrays if not provided by the institution.
   *
   * Note: This request may take some time to complete if identity was not specified as an initial
   * product when creating the Item. This is because Plaid must communicate directly with the institution
   * to retrieve the data.
   *
   * @param body
   * @return Response from the API call
   */
  async identityGet(
    body: IdentityGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<IdentityGetResponse>> {
    const req = this.createRequest('POST', '/identity/get');
    const mapped = req.prepareArgs({ body: [body, identityGetRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(identityGetResponseSchema, requestOptions);
  }
}
