/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AuthGetRequest,
  authGetRequestSchema,
} from '../models/authGetRequest.js';
import {
  AuthGetResponse,
  authGetResponseSchema,
} from '../models/authGetResponse.js';
import { BaseController } from './baseController.js';
import { ErrorError } from '../errors/errorError.js';

export class AuthController extends BaseController {
  /**
   * The `/auth/get` endpoint returns the bank account and bank identification numbers (such as routing
   * numbers, for US accounts) associated with an Item's checking and savings accounts, along with high-
   * level account data and balances when available.
   *
   * Note: This request may take some time to complete if `auth` was not specified as an initial product
   * when creating the Item. This is because Plaid must communicate directly with the institution to
   * retrieve the data.
   *
   * Also note that `/auth/get` will not return data for any new accounts opened after the Item was
   * created. To obtain data for new accounts, create a new Item.
   *
   * @param body
   * @return Response from the API call
   */
  async authGet(
    body: AuthGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AuthGetResponse>> {
    const req = this.createRequest('POST', '/auth/get');
    const mapped = req.prepareArgs({ body: [body, authGetRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Default error');
    req.authenticate([
      { pLAIDCLIENTID: true, pLAIDSECRET: true, plaidVersion: true },
    ]);
    return req.callAsJson(authGetResponseSchema, requestOptions);
  }
}
