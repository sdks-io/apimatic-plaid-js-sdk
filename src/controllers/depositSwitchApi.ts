/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  DepositSwitchAltCreateRequest,
  depositSwitchAltCreateRequestSchema,
} from '../models/depositSwitchAltCreateRequest.js';
import {
  DepositSwitchAltCreateResponse,
  depositSwitchAltCreateResponseSchema,
} from '../models/depositSwitchAltCreateResponse.js';
import {
  DepositSwitchCreateRequest,
  depositSwitchCreateRequestSchema,
} from '../models/depositSwitchCreateRequest.js';
import {
  DepositSwitchCreateResponse,
  depositSwitchCreateResponseSchema,
} from '../models/depositSwitchCreateResponse.js';
import {
  DepositSwitchGetRequest,
  depositSwitchGetRequestSchema,
} from '../models/depositSwitchGetRequest.js';
import {
  DepositSwitchGetResponse,
  depositSwitchGetResponseSchema,
} from '../models/depositSwitchGetResponse.js';
import {
  DepositSwitchTokenCreateRequest,
  depositSwitchTokenCreateRequestSchema,
} from '../models/depositSwitchTokenCreateRequest.js';
import {
  DepositSwitchTokenCreateResponse,
  depositSwitchTokenCreateResponseSchema,
} from '../models/depositSwitchTokenCreateResponse.js';
import { BaseApi } from './baseApi.js';

export class DepositSwitchApi extends BaseApi {
  /**
   * This endpoint returns information related to how the user has configured their payroll allocation
   * and the state of the switch. You can use this information to build logic related to the user's
   * direct deposit allocation preferences.
   *
   * @param body
   * @return Response from the API call
   */
  async depositSwitchGet(
    body: DepositSwitchGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DepositSwitchGetResponse>> {
    const req = this.createRequest('POST', '/deposit_switch/get');
    const mapped = req.prepareArgs({
      body: [body, depositSwitchGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(depositSwitchGetResponseSchema, requestOptions);
  }

  /**
   * This endpoint provides an alternative to `/deposit_switch/create` for customers who have not yet
   * fully integrated with Plaid Exchange. Like `/deposit_switch/create`, it creates a deposit switch
   * entity that will be persisted throughout the lifecycle of the switch.
   *
   * @param body
   * @return Response from the API call
   */
  async depositSwitchAltCreate(
    body: DepositSwitchAltCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DepositSwitchAltCreateResponse>> {
    const req = this.createRequest('POST', '/deposit_switch/alt/create');
    const mapped = req.prepareArgs({
      body: [body, depositSwitchAltCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(depositSwitchAltCreateResponseSchema, requestOptions);
  }

  /**
   * This endpoint creates a deposit switch entity that will be persisted throughout the lifecycle of the
   * switch.
   *
   * @param body
   * @return Response from the API call
   */
  async depositSwitchCreate(
    body: DepositSwitchCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DepositSwitchCreateResponse>> {
    const req = this.createRequest('POST', '/deposit_switch/create');
    const mapped = req.prepareArgs({
      body: [body, depositSwitchCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(depositSwitchCreateResponseSchema, requestOptions);
  }

  /**
   * In order for the end user to take action, you will need to create a public token representing the
   * deposit switch. This token is used to initialize Link. It can be used one time and expires after 30
   * minutes.
   *
   *
   * @param body
   * @return Response from the API call
   */
  async depositSwitchTokenCreate(
    body: DepositSwitchTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<DepositSwitchTokenCreateResponse>> {
    const req = this.createRequest('POST', '/deposit_switch/token/create');
    const mapped = req.prepareArgs({
      body: [body, depositSwitchTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      depositSwitchTokenCreateResponseSchema,
      requestOptions
    );
  }
}
