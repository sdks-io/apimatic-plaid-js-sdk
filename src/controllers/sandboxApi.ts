/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  SandboxBankTransferFireWebhookRequest,
  sandboxBankTransferFireWebhookRequestSchema,
} from '../models/sandboxBankTransferFireWebhookRequest.js';
import {
  SandboxBankTransferFireWebhookResponse,
  sandboxBankTransferFireWebhookResponseSchema,
} from '../models/sandboxBankTransferFireWebhookResponse.js';
import {
  SandboxBankTransferSimulateRequest,
  sandboxBankTransferSimulateRequestSchema,
} from '../models/sandboxBankTransferSimulateRequest.js';
import {
  SandboxBankTransferSimulateResponse,
  sandboxBankTransferSimulateResponseSchema,
} from '../models/sandboxBankTransferSimulateResponse.js';
import {
  SandboxIncomeFireWebhookRequest,
  sandboxIncomeFireWebhookRequestSchema,
} from '../models/sandboxIncomeFireWebhookRequest.js';
import {
  SandboxIncomeFireWebhookResponse,
  sandboxIncomeFireWebhookResponseSchema,
} from '../models/sandboxIncomeFireWebhookResponse.js';
import {
  SandboxItemFireWebhookRequest,
  sandboxItemFireWebhookRequestSchema,
} from '../models/sandboxItemFireWebhookRequest.js';
import {
  SandboxItemFireWebhookResponse,
  sandboxItemFireWebhookResponseSchema,
} from '../models/sandboxItemFireWebhookResponse.js';
import {
  SandboxItemResetLoginRequest,
  sandboxItemResetLoginRequestSchema,
} from '../models/sandboxItemResetLoginRequest.js';
import {
  SandboxItemResetLoginResponse,
  sandboxItemResetLoginResponseSchema,
} from '../models/sandboxItemResetLoginResponse.js';
import {
  SandboxItemSetVerificationStatusRequest,
  sandboxItemSetVerificationStatusRequestSchema,
} from '../models/sandboxItemSetVerificationStatusRequest.js';
import {
  SandboxItemSetVerificationStatusResponse,
  sandboxItemSetVerificationStatusResponseSchema,
} from '../models/sandboxItemSetVerificationStatusResponse.js';
import {
  SandboxOauthSelectAccountsRequest,
  sandboxOauthSelectAccountsRequestSchema,
} from '../models/sandboxOauthSelectAccountsRequest.js';
import {
  SandboxProcessorTokenCreateRequest,
  sandboxProcessorTokenCreateRequestSchema,
} from '../models/sandboxProcessorTokenCreateRequest.js';
import {
  SandboxProcessorTokenCreateResponse,
  sandboxProcessorTokenCreateResponseSchema,
} from '../models/sandboxProcessorTokenCreateResponse.js';
import {
  SandboxPublicTokenCreateRequest,
  sandboxPublicTokenCreateRequestSchema,
} from '../models/sandboxPublicTokenCreateRequest.js';
import {
  SandboxPublicTokenCreateResponse,
  sandboxPublicTokenCreateResponseSchema,
} from '../models/sandboxPublicTokenCreateResponse.js';
import {
  SandboxTransferSimulateRequest,
  sandboxTransferSimulateRequestSchema,
} from '../models/sandboxTransferSimulateRequest.js';
import {
  SandboxTransferSimulateResponse,
  sandboxTransferSimulateResponseSchema,
} from '../models/sandboxTransferSimulateResponse.js';
import { optional, unknown } from '../schema.js';
import { BaseApi } from './baseApi.js';
import { ErrorError } from '../errors/errorError.js';

export class SandboxApi extends BaseApi {
  /**
   * The `/sandbox/item/set_verification_status` endpoint can be used to change the verification status
   * of an Item in in the Sandbox in order to simulate the Automated Micro-deposit flow.
   *
   * Note that not all Plaid developer accounts are enabled for micro-deposit based verification by
   * default. Your account must be enabled for this feature in order to test it in Sandbox. To enable
   * this features or check your status, contact your account manager or [submit a product access Support
   * ticket](https://dashboard.plaid.com/support/new/product-and-development/product-
   * troubleshooting/request-product-access).
   *
   * For more information on testing Automated Micro-deposits in Sandbox, see [Auth full coverage
   * testing](https://plaid.com/docs/auth/coverage/testing#).
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxItemSetVerificationStatus(
    body: SandboxItemSetVerificationStatusRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxItemSetVerificationStatusResponse>> {
    const req = this.createRequest(
      'POST',
      '/sandbox/item/set_verification_status'
    );
    const mapped = req.prepareArgs({
      body: [body, sandboxItemSetVerificationStatusRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxItemSetVerificationStatusResponseSchema,
      requestOptions
    );
  }

  /**
   * Use the `/sandbox/public_token/create`  endpoint to create a valid `public_token`  for an arbitrary
   * institution ID, initial products, and test credentials. The created `public_token` maps to a new
   * Sandbox Item. You can then call `/item/public_token/exchange` to exchange the `public_token` for an
   * `access_token` and perform all API actions. `/sandbox/public_token/create` can also be used with the
   * [`user_custom` test username](https://plaid.com/docs/sandbox/user-custom) to generate a test account
   * with custom data.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxPublicTokenCreate(
    body: SandboxPublicTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxPublicTokenCreateResponse>> {
    const req = this.createRequest('POST', '/sandbox/public_token/create');
    const mapped = req.prepareArgs({
      body: [body, sandboxPublicTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxPublicTokenCreateResponseSchema,
      requestOptions
    );
  }

  /**
   * Use the `/sandbox/processor_token/create` endpoint to create a valid `processor_token` for an
   * arbitrary institution ID and test credentials. The created `processor_token` corresponds to a new
   * Sandbox Item. You can then use this `processor_token` with the `/processor/` API endpoints in
   * Sandbox. You can also use `/sandbox/processor_token/create` with the [`user_custom` test
   * username](https://plaid.com/docs/sandbox/user-custom) to generate a test account with custom data.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxProcessorTokenCreate(
    body: SandboxProcessorTokenCreateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxProcessorTokenCreateResponse>> {
    const req = this.createRequest('POST', '/sandbox/processor_token/create');
    const mapped = req.prepareArgs({
      body: [body, sandboxProcessorTokenCreateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxProcessorTokenCreateResponseSchema,
      requestOptions
    );
  }

  /**
   * The `/sandbox/item/fire_webhook` endpoint is used to test that code correctly handles webhooks.
   * Calling this endpoint triggers a Transactions `DEFAULT_UPDATE` webhook to be fired for a given
   * Sandbox Item. If the Item does not support Transactions, a `SANDBOX_PRODUCT_NOT_ENABLED` error will
   * result.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxItemFireWebhook(
    body: SandboxItemFireWebhookRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxItemFireWebhookResponse>> {
    const req = this.createRequest('POST', '/sandbox/item/fire_webhook');
    const mapped = req.prepareArgs({
      body: [body, sandboxItemFireWebhookRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(sandboxItemFireWebhookResponseSchema, requestOptions);
  }

  /**
   * `/sandbox/item/reset_login/` forces an Item into an `ITEM_LOGIN_REQUIRED` state in order to simulate
   * an Item whose login is no longer valid. This makes it easy to test Link's [update mode](https:
   * //plaid.com/docs/link/update-mode) flow in the Sandbox environment.  After calling
   * `/sandbox/item/reset_login`, You can then use Plaid Link update mode to restore the Item to a good
   * state. An `ITEM_LOGIN_REQUIRED` webhook will also be fired after a call to this endpoint, if one is
   * associated with the Item.
   *
   *
   * In the Sandbox, Items will transition to an `ITEM_LOGIN_REQUIRED` error state automatically after 30
   * days, even if this endpoint is not called.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxItemResetLogin(
    body: SandboxItemResetLoginRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxItemResetLoginResponse>> {
    const req = this.createRequest('POST', '/sandbox/item/reset_login');
    const mapped = req.prepareArgs({
      body: [body, sandboxItemResetLoginRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(sandboxItemResetLoginResponseSchema, requestOptions);
  }

  /**
   * Use the `/sandbox/income/fire_webhook` endpoint to manually trigger an Income webhook in the Sandbox
   * environment.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxIncomeFireWebhook(
    body: SandboxIncomeFireWebhookRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxIncomeFireWebhookResponse>> {
    const req = this.createRequest('POST', '/sandbox/income/fire_webhook');
    const mapped = req.prepareArgs({
      body: [body, sandboxIncomeFireWebhookRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxIncomeFireWebhookResponseSchema,
      requestOptions
    );
  }

  /**
   * Save the selected accounts when connecting to the Platypus Oauth institution
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxOauthSelectAccounts(
    body: SandboxOauthSelectAccountsRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<unknown | undefined>> {
    const req = this.createRequest('POST', '/sandbox/oauth/select_accounts');
    const mapped = req.prepareArgs({
      body: [body, sandboxOauthSelectAccountsRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(optional(unknown()), requestOptions);
  }

  /**
   * Use the `/sandbox/transfer/simulate` endpoint to simulate a transfer event in the Sandbox
   * environment.  Note that while an event will be simulated and will appear when using endpoints such
   * as `/transfer/event/sync` or `/transfer/event/list`, no transactions will actually take place and
   * funds will not move between accounts, even within the Sandbox.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxTransferSimulate(
    body: SandboxTransferSimulateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxTransferSimulateResponse>> {
    const req = this.createRequest('POST', '/sandbox/transfer/simulate');
    const mapped = req.prepareArgs({
      body: [body, sandboxTransferSimulateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxTransferSimulateResponseSchema,
      requestOptions
    );
  }

  /**
   * Use the `/sandbox/bank_transfer/fire_webhook` endpoint to manually trigger a Bank Transfers webhook
   * in the Sandbox environment.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxBankTransferFireWebhook(
    body: SandboxBankTransferFireWebhookRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxBankTransferFireWebhookResponse>> {
    const req = this.createRequest(
      'POST',
      '/sandbox/bank_transfer/fire_webhook'
    );
    const mapped = req.prepareArgs({
      body: [body, sandboxBankTransferFireWebhookRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxBankTransferFireWebhookResponseSchema,
      requestOptions
    );
  }

  /**
   * Use the `/sandbox/bank_transfer/simulate` endpoint to simulate a bank transfer event in the Sandbox
   * environment.  Note that while an event will be simulated and will appear when using endpoints such
   * as `/bank_transfer/event/sync` or `/bank_transfer/event/list`, no transactions will actually take
   * place and funds will not move between accounts, even within the Sandbox.
   *
   * @param body
   * @return Response from the API call
   */
  async sandboxBankTransferSimulate(
    body: SandboxBankTransferSimulateRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<SandboxBankTransferSimulateResponse>> {
    const req = this.createRequest('POST', '/sandbox/bank_transfer/simulate');
    const mapped = req.prepareArgs({
      body: [body, sandboxBankTransferSimulateRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(
      sandboxBankTransferSimulateResponseSchema,
      requestOptions
    );
  }
}
