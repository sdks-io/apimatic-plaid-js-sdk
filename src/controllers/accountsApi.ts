/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { ApiResponse, RequestOptions } from '../core.js';
import {
  AccountsBalanceGetRequest,
  accountsBalanceGetRequestSchema,
} from '../models/accountsBalanceGetRequest.js';
import {
  AccountsGetRequest,
  accountsGetRequestSchema,
} from '../models/accountsGetRequest.js';
import {
  AccountsGetResponse,
  accountsGetResponseSchema,
} from '../models/accountsGetResponse.js';
import { BaseApi } from './baseApi.js';
import { ErrorError } from '../errors/errorError.js';

export class AccountsApi extends BaseApi {
  /**
   * The `/accounts/get`  endpoint can be used to retrieve information for any linked Item. Note that
   * some information is nullable. Plaid will only return active bank accounts, i.e. accounts that are
   * not closed and are capable of carrying a balance.
   *
   * This endpoint retrieves cached information, rather than extracting fresh information from the
   * institution. As a result, balances returned may not be up-to-date; for realtime balance information,
   * use `/accounts/balance/get` instead.
   *
   * @param body
   * @return Response from the API call
   */
  async accountsGet(
    body: AccountsGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AccountsGetResponse>> {
    const req = this.createRequest('POST', '/accounts/get');
    const mapped = req.prepareArgs({ body: [body, accountsGetRequestSchema] });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.defaultToError(ErrorError, 'Error response.');
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(accountsGetResponseSchema, requestOptions);
  }

  /**
   * The `/accounts/balance/get` endpoint returns the real-time balance for each of an Item's accounts.
   * While other endpoints may return a balance object, only `/accounts/balance/get` forces the available
   * and current balance fields to be refreshed rather than cached. This endpoint can be used for
   * existing Items that were added via any of Plaid’s other products. This endpoint can be used as long
   * as Link has been initialized with any other product, `balance` itself is not a product that can be
   * used to initialize Link.
   *
   * @param body
   * @return Response from the API call
   */
  async accountsBalanceGet(
    body: AccountsBalanceGetRequest,
    requestOptions?: RequestOptions
  ): Promise<ApiResponse<AccountsGetResponse>> {
    const req = this.createRequest('POST', '/accounts/balance/get');
    const mapped = req.prepareArgs({
      body: [body, accountsBalanceGetRequestSchema],
    });
    req.header('Content-Type', 'application/json');
    req.json(mapped.body);
    req.authenticate([
      { plaidClientId: true, plaidSecret: true, plaidVersion: true },
    ]);
    return req.callAsJson(accountsGetResponseSchema, requestOptions);
  }
}
