/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  compositeAuthenticationProvider,
  customHeaderAuthenticationProvider,
} from './authentication.js';
import { Configuration } from './configuration.js';

export function createAuthProviderFromConfig(config: Partial<Configuration>) {
  const authConfig = {
    plaidClientId:
      config.plaidClientIdCredentials &&
      customHeaderAuthenticationProvider(config.plaidClientIdCredentials),
    plaidSecret:
      config.plaidSecretCredentials &&
      customHeaderAuthenticationProvider(config.plaidSecretCredentials),
    plaidVersion:
      config.plaidVersionCredentials &&
      customHeaderAuthenticationProvider(config.plaidVersionCredentials),
  };

  return compositeAuthenticationProvider<
    keyof typeof authConfig,
    typeof authConfig
  >(authConfig);
}
