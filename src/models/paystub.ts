/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Deductions, deductionsSchema } from './deductions.js';
import { Earnings, earningsSchema } from './earnings.js';
import { Employee, employeeSchema } from './employee.js';
import { Employer2, employer2Schema } from './employer2.js';
import {
  EmploymentDetails,
  employmentDetailsSchema,
} from './employmentDetails.js';
import { IncomeBreakdown, incomeBreakdownSchema } from './incomeBreakdown.js';
import { NetPay, netPaySchema } from './netPay.js';
import {
  PayPeriodDetails,
  payPeriodDetailsSchema,
} from './payPeriodDetails.js';
import { PaystubDetails, paystubDetailsSchema } from './paystubDetails.js';
import {
  PaystubYtdDetails,
  paystubYtdDetailsSchema,
} from './paystubYtdDetails.js';

/** An object representing data extracted from the end user's paystub. */
export interface Paystub {
  /** An object with the deduction information found on a paystub. */
  deductions?: Deductions;
  /** An identifier of the document referenced by the document metadata. */
  docId?: string;
  /** An object representing both a breakdown of earnings on a paystub and the total earnings. */
  earnings?: Earnings;
  employer: Employer2;
  /** Data about the employee. */
  employee: Employee;
  /** An object representing employment details found on a paystub. */
  employmentDetails?: EmploymentDetails;
  /** An object representing information about the net pay amount on the paystub. */
  netPay?: NetPay;
  /** Details about the pay period. */
  payPeriodDetails: PayPeriodDetails;
  /** An object representing details that can be found on the paystub. */
  paystubDetails?: PaystubDetails;
  incomeBreakdown: IncomeBreakdown[];
  /** The amount of income earned year to date, as based on paystub data. */
  ytdEarnings: PaystubYtdDetails;
  additionalProperties?: Record<string, unknown>;
}

export const paystubSchema: Schema<Paystub> = lazy(() =>
  typedExpandoObject(
    {
      deductions: ['deductions', optional(deductionsSchema)],
      docId: ['doc_id', optional(string())],
      earnings: ['earnings', optional(earningsSchema)],
      employer: ['employer', employer2Schema],
      employee: ['employee', employeeSchema],
      employmentDetails: [
        'employment_details',
        optional(employmentDetailsSchema),
      ],
      netPay: ['net_pay', optional(netPaySchema)],
      payPeriodDetails: ['pay_period_details', payPeriodDetailsSchema],
      paystubDetails: ['paystub_details', optional(paystubDetailsSchema)],
      incomeBreakdown: ['income_breakdown', array(incomeBreakdownSchema)],
      ytdEarnings: ['ytd_earnings', paystubYtdDetailsSchema],
    },
    'additionalProperties',
    optional(unknown())
  )
);
