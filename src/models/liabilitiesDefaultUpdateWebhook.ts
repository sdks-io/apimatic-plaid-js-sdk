/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  dict,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Error, errorSchema } from './error.js';

/** The webhook of type `LIABILITIES` and code `DEFAULT_UPDATE` will be fired when new or updated liabilities have been detected on a liabilities item. */
export interface LiabilitiesDefaultUpdateWebhook {
  /** `LIABILITIES` */
  webhookType: string;
  /** `DEFAULT_UPDATE` */
  webhookCode: string;
  /** The `item_id` of the Item associated with this webhook, warning, or error */
  itemId: string;
  /** We use standard HTTP response codes for success and failure notifications, and our errors are further classified by `error_type`. In general, 200 HTTP codes correspond to success, 40X codes are for developer- or user-related failures, and 50X codes are for Plaid-related issues.  Error fields will be `null` if no error has occurred. */
  error: Error;
  /** An array of `account_id`'s for accounts that contain new liabilities. */
  accountIdsWithNewLiabilities: string[];
  /**
   * An object with keys of `account_id`'s that are mapped to their respective liabilities fields that changed.
   * Example: `{ "XMBvvyMGQ1UoLbKByoMqH3nXMj84ALSdE5B58": ["past_amount_due"] }`
   */
  accountIdsWithUpdatedLiabilities?: Record<string, unknown>;
  additionalProperties?: Record<string, unknown>;
}

export const liabilitiesDefaultUpdateWebhookSchema: Schema<LiabilitiesDefaultUpdateWebhook> = lazy(
  () =>
    typedExpandoObject(
      {
        webhookType: ['webhook_type', string()],
        webhookCode: ['webhook_code', string()],
        itemId: ['item_id', string()],
        error: ['error', errorSchema],
        accountIdsWithNewLiabilities: [
          'account_ids_with_new_liabilities',
          array(string()),
        ],
        accountIdsWithUpdatedLiabilities: [
          'account_ids_with_updated_liabilities',
          optional(dict(unknown())),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
