/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** AssetReportAuditCopyCreateRequest defines the request schema for `/asset_report/audit_copy/get` */
export interface AssetReportAuditCopyCreateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** A token that can be provided to endpoints such as `/asset_report/get` or `/asset_report/pdf/get` to fetch or update an Asset Report. */
  assetReportToken: string;
  /** The `auditor_id` of the third party with whom you would like to share the Asset Report. */
  auditorId: string;
  additionalProperties?: Record<string, unknown>;
}

export const assetReportAuditCopyCreateRequestSchema: Schema<AssetReportAuditCopyCreateRequest> = typedExpandoObject(
  {
    clientId: ['client_id', optional(string())],
    secret: ['secret', optional(string())],
    assetReportToken: ['asset_report_token', string()],
    auditorId: ['auditor_id', string()],
  },
  'additionalProperties',
  optional(unknown())
);
