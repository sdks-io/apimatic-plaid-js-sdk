/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, number, object, optional, Schema, string } from '../schema.js';
import { Error, errorSchema } from './error.js';

/** Fired when new or updated holdings have been detected on an investment account. The webhook typically fires once per day, after market close, in response to any newly added holdings or price changes to existing holdings. */
export interface HoldingsDefaultUpdateWebhook {
  /** `HOLDINGS` */
  webhookType: string;
  /** `DEFAULT_UPDATE` */
  webhookCode: string;
  /** The `item_id` of the Item associated with this webhook, warning, or error */
  itemId: string;
  /** We use standard HTTP response codes for success and failure notifications, and our errors are further classified by `error_type`. In general, 200 HTTP codes correspond to success, 40X codes are for developer- or user-related failures, and 50X codes are for Plaid-related issues.  Error fields will be `null` if no error has occurred. */
  error?: Error;
  /** The number of new holdings reported since the last time this webhook was fired. */
  newHoldings: number;
  /** The number of updated holdings reported since the last time this webhook was fired. */
  updatedHoldings: number;
}

export const holdingsDefaultUpdateWebhookSchema: Schema<HoldingsDefaultUpdateWebhook> = lazy(
  () =>
    object({
      webhookType: ['webhook_type', string()],
      webhookCode: ['webhook_code', string()],
      itemId: ['item_id', string()],
      error: ['error', optional(errorSchema)],
      newHoldings: ['new_holdings', number()],
      updatedHoldings: ['updated_holdings', number()],
    })
);
