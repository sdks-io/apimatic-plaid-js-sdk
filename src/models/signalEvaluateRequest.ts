/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  SignalEvaluateDevice,
  signalEvaluateDeviceSchema,
} from './signalEvaluateDevice.js';
import { SignalUser, signalUserSchema } from './signalUser.js';

/** SignalEvaluateRequest defines the request schema for `/signal/evaluate` */
export interface SignalEvaluateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The access token associated with the Item data is being requested for. */
  accessToken: string;
  /** The `account_id` of the account whose verification status is to be modified */
  accountId: string;
  /** The unique ID that you would like to use to refer to this transaction. For your convenience mapping your internal data, you could use your internal ID/identifier for this transaction. The max length for this field is 36 characters. */
  clientTransactionId: string;
  /** The transaction amount, in USD (e.g. `102.05`) */
  amount: number;
  /** A unique ID that identifies the end user in your system. This ID is used to correlate requests by a user with multiple Items. The max length for this field is 36 characters. */
  clientUserId?: string;
  /** Details about the end user initiating the transaction (i.e., the account holder). */
  user?: SignalUser;
  /** Details about the end user's device */
  device?: SignalEvaluateDevice;
  additionalProperties?: Record<string, unknown>;
}

export const signalEvaluateRequestSchema: Schema<SignalEvaluateRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        accessToken: ['access_token', string()],
        accountId: ['account_id', string()],
        clientTransactionId: ['client_transaction_id', string()],
        amount: ['amount', number()],
        clientUserId: ['client_user_id', optional(string())],
        user: ['user', optional(signalUserSchema)],
        device: ['device', optional(signalEvaluateDeviceSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
