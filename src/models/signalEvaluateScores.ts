/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  BankInitiatedReturnRisk,
  bankInitiatedReturnRiskSchema,
} from './bankInitiatedReturnRisk.js';
import {
  CustomerInitiatedReturnRisk,
  customerInitiatedReturnRiskSchema,
} from './customerInitiatedReturnRisk.js';

/** Risk scoring details broken down by risk category. */
export interface SignalEvaluateScores {
  /** The object contains a risk score and a risk tier that evaluate the transaction return risk of an unauthorized debit. Common return codes in this category include: “R05”, "R07", "R10", "R11", "R29". These returns typically have a return time frame of up to 60 calendar days. During this period, the customer of financial institutions can dispute a transaction as unauthorized. */
  customerInitiatedReturnRisk?: CustomerInitiatedReturnRisk;
  /** The object contains a risk score and a risk tier that evaluate the transaction return risk because an account is overdrawn or because an ineligible account is used. Common return codes in this category include: "R01", "R02", "R03", "R04", "R06", “R08”,  "R09", "R13", "R16", "R17", "R20", "R23". These returns have a turnaround time of 2 banking days. */
  bankInitiatedReturnRisk?: BankInitiatedReturnRisk;
  additionalProperties?: Record<string, unknown>;
}

export const signalEvaluateScoresSchema: Schema<SignalEvaluateScores> = lazy(
  () =>
    typedExpandoObject(
      {
        customerInitiatedReturnRisk: [
          'customer_initiated_return_risk',
          optional(customerInitiatedReturnRiskSchema),
        ],
        bankInitiatedReturnRisk: [
          'bank_initiated_return_risk',
          optional(bankInitiatedReturnRiskSchema),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
