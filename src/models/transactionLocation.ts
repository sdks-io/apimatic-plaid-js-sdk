/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  nullable,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** A representation of where a transaction took place */
export interface TransactionLocation {
  /** The street address where the transaction occurred. */
  address: string | null;
  /** The city where the transaction occurred. */
  city: string | null;
  /** The region or state where the transaction occurred. */
  region: string | null;
  /** The postal code where the transaction occurred. */
  postalCode: string | null;
  /** The ISO 3166-1 alpha-2 country code where the transaction occurred. */
  country: string | null;
  /** The latitude where the transaction occurred. */
  lat: number | null;
  /** The longitude where the transaction occurred. */
  lon: number | null;
  /** The merchant defined store number where the transaction occurred. */
  storeNumber: string | null;
  additionalProperties?: Record<string, unknown>;
}

export const transactionLocationSchema: Schema<TransactionLocation> = typedExpandoObject(
  {
    address: ['address', nullable(string())],
    city: ['city', nullable(string())],
    region: ['region', nullable(string())],
    postalCode: ['postal_code', nullable(string())],
    country: ['country', nullable(string())],
    lat: ['lat', nullable(number())],
    lon: ['lon', nullable(number())],
    storeNumber: ['store_number', nullable(string())],
  },
  'additionalProperties',
  optional(unknown())
);
