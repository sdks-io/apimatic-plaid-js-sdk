/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  SandboxProcessorTokenCreateRequestOptions,
  sandboxProcessorTokenCreateRequestOptionsSchema,
} from './sandboxProcessorTokenCreateRequestOptions.js';

export interface SandboxProcessorTokenCreateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The ID of the institution the Item will be associated with */
  institutionId: string;
  /** An optional set of options to be used when configuring the Item. If specified, must not be `null`. */
  options?: SandboxProcessorTokenCreateRequestOptions;
  additionalProperties?: Record<string, unknown>;
}

export const sandboxProcessorTokenCreateRequestSchema: Schema<SandboxProcessorTokenCreateRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        institutionId: ['institution_id', string()],
        options: [
          'options',
          optional(sandboxProcessorTokenCreateRequestOptionsSchema),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
