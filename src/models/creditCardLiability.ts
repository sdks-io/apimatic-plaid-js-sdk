/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  nullable,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Apr, aprSchema } from './apr.js';

/** An object representing a credit card account. */
export interface CreditCardLiability {
  /** The ID of the account that this liability belongs to. */
  accountId: string | null;
  /** The various interest rates that apply to the account. */
  aprs: Apr[];
  /** true if a payment is currently overdue. Availability for this field is limited. */
  isOverdue: boolean | null;
  /** The amount of the last payment. */
  lastPaymentAmount: number;
  /** The date of the last payment. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD). Availability for this field is limited. */
  lastPaymentDate: string;
  /** The date of the last statement. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD). */
  lastStatementIssueDate: string;
  /** The minimum payment due for the next billing cycle. */
  minimumPaymentAmount: number;
  /** The due date for the next payment. The due date is `null` if a payment is not expected. Dates are returned in an [ISO 8601](https://wikipedia.org/wiki/ISO_8601) format (YYYY-MM-DD). */
  nextPaymentDueDate: string | null;
  additionalProperties?: Record<string, unknown>;
}

export const creditCardLiabilitySchema: Schema<CreditCardLiability> = lazy(() =>
  typedExpandoObject(
    {
      accountId: ['account_id', nullable(string())],
      aprs: ['aprs', array(aprSchema)],
      isOverdue: ['is_overdue', nullable(boolean())],
      lastPaymentAmount: ['last_payment_amount', number()],
      lastPaymentDate: ['last_payment_date', string()],
      lastStatementIssueDate: ['last_statement_issue_date', string()],
      minimumPaymentAmount: ['minimum_payment_amount', number()],
      nextPaymentDueDate: ['next_payment_due_date', nullable(string())],
    },
    'additionalProperties',
    optional(unknown())
  )
);
