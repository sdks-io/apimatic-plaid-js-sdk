/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema.js';
import {
  BankTransferFailure,
  bankTransferFailureSchema,
} from './bankTransferFailure.js';

/** Defines the request schema for `/sandbox/bank_transfer/simulate` */
export interface SandboxBankTransferSimulateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** Plaid’s unique identifier for a bank transfer. */
  bankTransferId: string;
  /**
   * The asynchronous event to be simulated. May be: `posted`, `failed`, or `reversed`.
   * An error will be returned if the event type is incompatible with the current transfer status. Compatible status --> event type transitions include:
   * `pending` --> `failed`
   * `pending` --> `posted`
   * `posted` --> `reversed`
   */
  eventType: string;
  /** The failure reason if the type of this transfer is `"failed"` or `"reversed"`. Null value otherwise. */
  failureReason?: BankTransferFailure;
}

export const sandboxBankTransferSimulateRequestSchema: Schema<SandboxBankTransferSimulateRequest> = lazy(
  () =>
    object({
      clientId: ['client_id', optional(string())],
      secret: ['secret', optional(string())],
      bankTransferId: ['bank_transfer_id', string()],
      eventType: ['event_type', string()],
      failureReason: ['failure_reason', optional(bankTransferFailureSchema)],
    })
);
