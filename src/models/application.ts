/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** Metadata about the application */
export interface Application {
  /** This field will map to the application ID that is returned from /item/applications/list, or provided to the institution in an oauth redirect. */
  applicationId: string;
  /** The name of the application */
  name: string;
  /** The date this application was linked in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) (YYYY-MM-DD) format in UTC. */
  createdAt: string;
  /** A URL that links to the application logo image. */
  logoUrl: string | null;
  /** The URL for the application's website */
  applicationUrl: string | null;
  /** A string provided by the connected app stating why they use their respective enabled products. */
  reasonForAccess: string | null;
  additionalProperties?: Record<string, unknown>;
}

export const applicationSchema: Schema<Application> = typedExpandoObject(
  {
    applicationId: ['application_id', string()],
    name: ['name', string()],
    createdAt: ['created_at', string()],
    logoUrl: ['logo_url', nullable(string())],
    applicationUrl: ['application_url', nullable(string())],
    reasonForAccess: ['reason_for_access', nullable(string())],
  },
  'additionalProperties',
  optional(unknown())
);
