/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  DistributionDetails,
  distributionDetailsSchema,
} from './distributionDetails.js';
import { Total, totalSchema } from './total.js';

/** An object representing information about the net pay amount on the paystub. */
export interface NetPay {
  distributionDetails?: DistributionDetails[];
  /** An object representing both the current pay period and year to date amount for a category. */
  total?: Total;
  additionalProperties?: Record<string, unknown>;
}

export const netPaySchema: Schema<NetPay> = lazy(() =>
  typedExpandoObject(
    {
      distributionDetails: [
        'distribution_details',
        optional(array(distributionDetailsSchema)),
      ],
      total: ['total', optional(totalSchema)],
    },
    'additionalProperties',
    optional(unknown())
  )
);
