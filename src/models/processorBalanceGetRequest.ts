/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema.js';
import {
  ProcessorBalanceGetRequestOptions,
  processorBalanceGetRequestOptionsSchema,
} from './processorBalanceGetRequestOptions.js';

/** ProcessorBalanceGetRequest defines the request schema for `/processor/balance/get` */
export interface ProcessorBalanceGetRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The processor token obtained from the Plaid integration partner. Processor tokens are in the format: `processor-<environment>-<identifier>` */
  processorToken: string;
  /** An optional object to filter `/processor/balance/get` results. */
  options?: ProcessorBalanceGetRequestOptions;
}

export const processorBalanceGetRequestSchema: Schema<ProcessorBalanceGetRequest> = lazy(
  () =>
    object({
      clientId: ['client_id', optional(string())],
      secret: ['secret', optional(string())],
      processorToken: ['processor_token', string()],
      options: ['options', optional(processorBalanceGetRequestOptionsSchema)],
    })
);
