/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { CreditFilter, creditFilterSchema } from './creditFilter.js';
import {
  DepositoryFilter,
  depositoryFilterSchema,
} from './depositoryFilter.js';
import {
  InvestmentFilter,
  investmentFilterSchema,
} from './investmentFilter.js';
import { LoanFilter, loanFilterSchema } from './loanFilter.js';

/** The `account_filters` specified in the original call to `/link/token/create`. */
export interface AccountFiltersResponse {
  /** A filter to apply to `depository`-type accounts */
  depository?: DepositoryFilter;
  /** A filter to apply to `credit`-type accounts */
  credit?: CreditFilter;
  /** A filter to apply to `loan`-type accounts */
  loan?: LoanFilter;
  /** A filter to apply to `investment`-type accounts */
  investment?: InvestmentFilter;
  additionalProperties?: Record<string, unknown>;
}

export const accountFiltersResponseSchema: Schema<AccountFiltersResponse> = lazy(
  () =>
    typedExpandoObject(
      {
        depository: ['depository', optional(depositoryFilterSchema)],
        credit: ['credit', optional(creditFilterSchema)],
        loan: ['loan', optional(loanFilterSchema)],
        investment: ['investment', optional(investmentFilterSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
