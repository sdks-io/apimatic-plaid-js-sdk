/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { EarningsTotal, earningsTotalSchema } from './earningsTotal.js';

/** An object representing both a breakdown of earnings on a paystub and the total earnings. */
export interface Earnings {
  subtotals?: EarningsTotal[];
  totals?: EarningsTotal[];
  additionalProperties?: Record<string, unknown>;
}

export const earningsSchema: Schema<Earnings> = lazy(() =>
  typedExpandoObject(
    {
      subtotals: ['subtotals', optional(array(earningsTotalSchema))],
      totals: ['totals', optional(array(earningsTotalSchema))],
    },
    'additionalProperties',
    optional(unknown())
  )
);
