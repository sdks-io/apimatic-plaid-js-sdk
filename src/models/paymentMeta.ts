/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { nullable, object, Schema, string } from '../schema.js';

/**
 * Transaction information specific to inter-bank transfers. If the transaction was not an inter-bank transfer, all fields will be `null`.
 * If the `transactions` object was returned by a Transactions endpoint such as `/transactions/get`, the `payment_meta` key will always appear, but no data elements are guaranteed. If the `transactions` object was returned by an Assets endpoint such as `/asset_report/get/` or `/asset_report/pdf/get`, this field will only appear in an Asset Report with Insights.
 */
export interface PaymentMeta {
  /** The transaction reference number supplied by the financial institution. */
  referenceNumber: string | null;
  /** The ACH PPD ID for the payer. */
  ppdId: string | null;
  /** For transfers, the party that is receiving the transaction. */
  payee: string | null;
  /** The party initiating a wire transfer. Will be `null` if the transaction is not a wire transfer. */
  byOrderOf: string | null;
  /** For transfers, the party that is paying the transaction. */
  payer: string | null;
  /** The type of transfer, e.g. 'ACH' */
  paymentMethod: string | null;
  /** The name of the payment processor */
  paymentProcessor: string | null;
  /** The payer-supplied description of the transfer. */
  reason: string | null;
}

export const paymentMetaSchema: Schema<PaymentMeta> = object({
  referenceNumber: ['reference_number', nullable(string())],
  ppdId: ['ppd_id', nullable(string())],
  payee: ['payee', nullable(string())],
  byOrderOf: ['by_order_of', nullable(string())],
  payer: ['payer', nullable(string())],
  paymentMethod: ['payment_method', nullable(string())],
  paymentProcessor: ['payment_processor', nullable(string())],
  reason: ['reason', nullable(string())],
});
