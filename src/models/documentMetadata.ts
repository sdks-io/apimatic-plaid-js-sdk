/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** An object representing metadata from the end user's uploaded document. */
export interface DocumentMetadata {
  /** The name of the document. */
  name?: string;
  /** The processing status of the document. */
  status?: string;
  /** An identifier of the document that is also present in the paystub response. */
  docId?: string;
  additionalProperties?: Record<string, unknown>;
}

export const documentMetadataSchema: Schema<DocumentMetadata> = typedExpandoObject(
  {
    name: ['name', optional(string())],
    status: ['status', optional(string())],
    docId: ['doc_id', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
