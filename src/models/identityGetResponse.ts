/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { AccountIdentity, accountIdentitySchema } from './accountIdentity.js';
import { Item, itemSchema } from './item.js';

/** IdentityGetResponse defines the response schema for `/identity/get` */
export interface IdentityGetResponse {
  /** The accounts for which Identity data has been requested */
  accounts: AccountIdentity[];
  /** Metadata about the Item. */
  item: Item;
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId: string;
  additionalProperties?: Record<string, unknown>;
}

export const identityGetResponseSchema: Schema<IdentityGetResponse> = lazy(() =>
  typedExpandoObject(
    {
      accounts: ['accounts', array(accountIdentitySchema)],
      item: ['item', itemSchema],
      requestId: ['request_id', string()],
    },
    'additionalProperties',
    optional(unknown())
  )
);
