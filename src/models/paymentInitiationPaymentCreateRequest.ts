/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  ExternalPaymentScheduleRequest,
  externalPaymentScheduleRequestSchema,
} from './externalPaymentScheduleRequest.js';
import { PaymentAmount, paymentAmountSchema } from './paymentAmount.js';
import { PaymentOptions, paymentOptionsSchema } from './paymentOptions.js';

/** PaymentInitiationPaymentCreateRequest defines the request schema for `/payment_initiation/payment/create` */
export interface PaymentInitiationPaymentCreateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The ID of the recipient the payment is for. */
  recipientId: string;
  /** A reference for the payment. This must be an alphanumeric string with at most 18 characters and must not contain any special characters (since not all institutions support them). */
  reference: string;
  /** The amount and currency of a payment */
  amount: PaymentAmount;
  /** The schedule that the payment will be executed on. If a schedule is provided, the payment is automatically set up as a standing order. If no schedule is specified, the payment will be executed only once. */
  schedule?: ExternalPaymentScheduleRequest;
  /** Additional payment options */
  options?: PaymentOptions;
  additionalProperties?: Record<string, unknown>;
}

export const paymentInitiationPaymentCreateRequestSchema: Schema<PaymentInitiationPaymentCreateRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        recipientId: ['recipient_id', string()],
        reference: ['reference', string()],
        amount: ['amount', paymentAmountSchema],
        schedule: ['schedule', optional(externalPaymentScheduleRequestSchema)],
        options: ['options', optional(paymentOptionsSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
