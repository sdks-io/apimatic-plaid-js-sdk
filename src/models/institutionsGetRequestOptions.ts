/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Products, productsSchema } from './products.js';

/** An optional object to filter `/institutions/get` results. */
export interface InstitutionsGetRequestOptions {
  /** Filter the Institutions based on which products they support. */
  products?: Products[];
  /** Specify an array of routing numbers to filter institutions. The response will only return institutions that match all of the routing numbers in the array. */
  routingNumbers?: string[];
  /** Limit results to institutions with or without OAuth login flows. This is primarily relevant to institutions with European country codes. */
  oauth?: boolean;
  /**
   * When `true`, return the institution's homepage URL, logo and primary brand color.
   * Note that Plaid does not own any of the logos shared by the API, and that by accessing or using these logos, you agree that you are doing so at your own risk and will, if necessary, obtain all required permissions from the appropriate rights holders and adhere to any applicable usage guidelines. Plaid disclaims all express or implied warranties with respect to the logos.
   */
  includeOptionalMetadata?: boolean;
  /** When `true`, returns metadata related to the Auth product indicating which auth methods are supported. */
  includeAuthMetadata?: boolean;
  /** When `true`, returns metadata related to the Payment Initiation product indicating which payment configurations are supported. */
  includePaymentInitiationMetadata?: boolean;
  additionalProperties?: Record<string, unknown>;
}

export const institutionsGetRequestOptionsSchema: Schema<InstitutionsGetRequestOptions> = typedExpandoObject(
  {
    products: ['products', optional(array(productsSchema))],
    routingNumbers: ['routing_numbers', optional(array(string()))],
    oauth: ['oauth', optional(boolean())],
    includeOptionalMetadata: ['include_optional_metadata', optional(boolean())],
    includeAuthMetadata: ['include_auth_metadata', optional(boolean())],
    includePaymentInitiationMetadata: [
      'include_payment_initiation_metadata',
      optional(boolean()),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
