/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  DepositSwitchAddressData,
  depositSwitchAddressDataSchema,
} from './depositSwitchAddressData.js';

export interface DepositSwitchTargetUser {
  /** The given name (first name) of the user. */
  givenName: string;
  /** The family name (last name) of the user. */
  familyName: string;
  /** The phone number of the user. The endpoint can accept a variety of phone number formats, including E.164. */
  phone: string;
  /** The email address of the user. */
  email: string;
  /** The user's address. */
  address?: DepositSwitchAddressData;
  /** The taxpayer ID of the user, generally their SSN, EIN, or TIN. */
  taxPayerId?: string;
  additionalProperties?: Record<string, unknown>;
}

export const depositSwitchTargetUserSchema: Schema<DepositSwitchTargetUser> = lazy(
  () =>
    typedExpandoObject(
      {
        givenName: ['given_name', string()],
        familyName: ['family_name', string()],
        phone: ['phone', string()],
        email: ['email', string()],
        address: ['address', optional(depositSwitchAddressDataSchema)],
        taxPayerId: ['tax_payer_id', optional(string())],
      },
      'additionalProperties',
      optional(unknown())
    )
);
