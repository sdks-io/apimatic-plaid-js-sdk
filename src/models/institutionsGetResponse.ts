/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, number, object, Schema, string } from '../schema.js';
import { Institution, institutionSchema } from './institution.js';

/** InstitutionsGetResponse defines the response schema for `/institutions/get` */
export interface InstitutionsGetResponse {
  /** A list of Plaid Institution */
  institutions: Institution[];
  /** The total number of institutions available via this endpoint */
  total: number;
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId: string;
}

export const institutionsGetResponseSchema: Schema<InstitutionsGetResponse> = lazy(
  () =>
    object({
      institutions: ['institutions', array(institutionSchema)],
      total: ['total', number()],
      requestId: ['request_id', string()],
    })
);
