/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  number,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  VerificationStatus,
  verificationStatusSchema,
} from './verificationStatus.js';

export interface YtdGrossIncomeSummaryFieldNumber {
  /** The value of the field. */
  value: number;
  /**
   * The verification status. One of the following:
   * `"VERIFIED"`: The information was successfully verified.
   * `"UNVERIFIED"`: The verification has not yet been performed.
   * `"NEEDS_INFO"`: The verification was attempted but could not be completed due to missing information.
   * "`UNABLE_TO_VERIFY`": The verification was performed and the information could not be verified.
   * `"UNKNOWN"`: The verification status is unknown.
   */
  verificationStatus: VerificationStatus;
  additionalProperties?: Record<string, unknown>;
}

export const ytdGrossIncomeSummaryFieldNumberSchema: Schema<YtdGrossIncomeSummaryFieldNumber> = typedExpandoObject(
  {
    value: ['value', number()],
    verificationStatus: ['verification_status', verificationStatusSchema],
  },
  'additionalProperties',
  optional(unknown())
);
