/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  nullable,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** An optional object to be used with the request. If specified, `options` must not be `null`. */
export interface TransactionsGetRequestOptions {
  /**
   * A list of `account_ids` to retrieve for the Item
   * Note: An error will be returned if a provided `account_id` is not associated with the Item.
   */
  accountIds?: string[];
  /** The number of transactions to fetch. */
  count?: number;
  /** The number of transactions to skip. The default value is 0. */
  offset?: number;
  /** Include the raw unparsed transaction description from the financial institution. This field is disabled by default. If you need this information in addition to the parsed data provided, contact your Plaid Account Manager. */
  includeOriginalDescription?: boolean | null;
  /** Include the `personal_finance_category` object in the response. This feature is currently in beta – to request access, contact transactions-feedback@plaid.com. */
  includePersonalFinanceCategoryBeta?: boolean;
  additionalProperties?: Record<string, unknown>;
}

export const transactionsGetRequestOptionsSchema: Schema<TransactionsGetRequestOptions> = typedExpandoObject(
  {
    accountIds: ['account_ids', optional(array(string()))],
    count: ['count', optional(number())],
    offset: ['offset', optional(number())],
    includeOriginalDescription: [
      'include_original_description',
      optional(nullable(boolean())),
    ],
    includePersonalFinanceCategoryBeta: [
      'include_personal_finance_category_beta',
      optional(boolean()),
    ],
  },
  'additionalProperties',
  optional(unknown())
);
