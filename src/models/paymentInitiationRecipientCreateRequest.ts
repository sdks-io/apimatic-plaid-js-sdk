/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  PaymentInitiationAddress,
  paymentInitiationAddressSchema,
} from './paymentInitiationAddress.js';
import {
  RecipientBacsNullable,
  recipientBacsNullableSchema,
} from './recipientBacsNullable.js';

/** PaymentInitiationRecipientCreateRequest defines the request schema for `/payment_initiation/recipient/create` */
export interface PaymentInitiationRecipientCreateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The name of the recipient */
  name: string;
  /** The International Bank Account Number (IBAN) for the recipient. If BACS data is not provided, an IBAN is required. */
  iban?: string | null;
  bacs?: RecipientBacsNullable;
  /** The optional address of the payment recipient. This object is not currently required to make payments from UK institutions and should not be populated, though may be necessary for future European expansion. */
  address?: PaymentInitiationAddress;
  additionalProperties?: Record<string, unknown>;
}

export const paymentInitiationRecipientCreateRequestSchema: Schema<PaymentInitiationRecipientCreateRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        name: ['name', string()],
        iban: ['iban', optional(nullable(string()))],
        bacs: ['bacs', optional(recipientBacsNullableSchema)],
        address: ['address', optional(paymentInitiationAddressSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
