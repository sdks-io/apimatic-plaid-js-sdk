/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { object, optional, Schema, string } from '../schema.js';
import {
  VerificationStatus3Enum,
  verificationStatus3EnumSchema,
} from './verificationStatus3Enum.js';

/** SandboxIncomeFireWebhookRequest defines the request schema for `/sandbox/income/fire_webhook` */
export interface SandboxIncomeFireWebhookRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The ID of the verification. */
  incomeVerificationId: string;
  /** The URL to which the webhook should be sent. */
  webhook: string;
  /**
   * `VERIFICATION_STATUS_PROCESSING_COMPLETE`: The income verification status processing has completed.
   * `VERIFICATION_STATUS_DOCUMENT_REJECTED`: The documentation uploaded by the end user was recognized as a supported file format, but not recognized as a valid paystub.
   * `VERIFICATION_STATUS_PROCESSING_FAILED`: A failure occurred when attempting to process the verification documentation.
   */
  verificationStatus: VerificationStatus3Enum;
}

export const sandboxIncomeFireWebhookRequestSchema: Schema<SandboxIncomeFireWebhookRequest> = object(
  {
    clientId: ['client_id', optional(string())],
    secret: ['secret', optional(string())],
    incomeVerificationId: ['income_verification_id', string()],
    webhook: ['webhook', string()],
    verificationStatus: ['verification_status', verificationStatus3EnumSchema],
  }
);
