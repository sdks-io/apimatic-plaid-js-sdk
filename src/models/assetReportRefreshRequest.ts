/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  AssetReportRefreshRequestOptions,
  assetReportRefreshRequestOptionsSchema,
} from './assetReportRefreshRequestOptions.js';

/** AssetReportRefreshRequest defines the request schema for `/asset_report/refresh` */
export interface AssetReportRefreshRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The `asset_report_token` returned by the original call to `/asset_report/create` */
  assetReportToken: string;
  /** The maximum number of days of history to include in the Asset Report. Must be an integer. If not specified, the value from the original call to `/asset_report/create` will be used. */
  daysRequested?: number;
  /** An optional object to filter `/asset_report/refresh` results. If provided, cannot be `null`. If not specified, the `options` from the original call to `/asset_report/create` will be used. */
  options?: AssetReportRefreshRequestOptions;
  additionalProperties?: Record<string, unknown>;
}

export const assetReportRefreshRequestSchema: Schema<AssetReportRefreshRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        assetReportToken: ['asset_report_token', string()],
        daysRequested: ['days_requested', optional(number())],
        options: ['options', optional(assetReportRefreshRequestOptionsSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
