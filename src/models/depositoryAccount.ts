/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** An account type holding cash, in which funds are deposited. Supported products for `depository` accounts are: Auth, Balance, Transactions, Identity, Payment Initiation, and Assets. */
export interface DepositoryAccount {
  /** Checking account */
  checking: string;
  /** Savings account */
  savings: string;
  /** Health Savings Account (US only) that can only hold cash */
  hsa: string;
  /** Certificate of deposit account */
  cd: string;
  /** Money market account */
  moneyMarket: string;
  /** PayPal depository account */
  paypal: string;
  /** Prepaid debit card */
  prepaid: string;
  /** A cash management account, typically a cash account at a brokerage */
  cashManagement: string;
  /** An Electronic Benefit Transfer (EBT) account, used by certain public assistance programs to distribute funds (US only) */
  ebt: string;
  additionalProperties?: Record<string, unknown>;
}

export const depositoryAccountSchema: Schema<DepositoryAccount> = typedExpandoObject(
  {
    checking: ['checking', string()],
    savings: ['savings', string()],
    hsa: ['hsa', string()],
    cd: ['cd', string()],
    moneyMarket: ['money market', string()],
    paypal: ['paypal', string()],
    prepaid: ['prepaid', string()],
    cashManagement: ['cash management', string()],
    ebt: ['ebt', string()],
  },
  'additionalProperties',
  optional(unknown())
);
