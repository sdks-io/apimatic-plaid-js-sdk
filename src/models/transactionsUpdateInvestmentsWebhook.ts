/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Error, errorSchema } from './error.js';

/** Fired when new or canceled transactions have been detected on an investment account. */
export interface TransactionsUpdateInvestmentsWebhook {
  /** `INVESTMENTS_TRANSACTIONS` */
  webhookType: string;
  /** `DEFAULT_UPDATE` */
  webhookCode: string;
  /** The `item_id` of the Item associated with this webhook, warning, or error */
  itemId: string;
  /** We use standard HTTP response codes for success and failure notifications, and our errors are further classified by `error_type`. In general, 200 HTTP codes correspond to success, 40X codes are for developer- or user-related failures, and 50X codes are for Plaid-related issues.  Error fields will be `null` if no error has occurred. */
  error?: Error;
  /** The number of new transactions reported since the last time this webhook was fired. */
  newInvestmentsTransactions: number;
  /** The number of canceled transactions reported since the last time this webhook was fired. */
  canceledInvestmentsTransactions: number;
  additionalProperties?: Record<string, unknown>;
}

export const transactionsUpdateInvestmentsWebhookSchema: Schema<TransactionsUpdateInvestmentsWebhook> = lazy(
  () =>
    typedExpandoObject(
      {
        webhookType: ['webhook_type', string()],
        webhookCode: ['webhook_code', string()],
        itemId: ['item_id', string()],
        error: ['error', optional(errorSchema)],
        newInvestmentsTransactions: ['new_investments_transactions', number()],
        canceledInvestmentsTransactions: [
          'canceled_investments_transactions',
          number(),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
