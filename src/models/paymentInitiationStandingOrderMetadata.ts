/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, boolean, object, Schema } from '../schema.js';
import {
  PaymentScheduleIntervalEnum,
  paymentScheduleIntervalEnumSchema,
} from './paymentScheduleIntervalEnum.js';

/** Metadata specifically related to valid Payment Initiation standing order configurations for the institution. */
export interface PaymentInitiationStandingOrderMetadata {
  /** Indicates whether the institution supports closed-ended standing orders by providing an end date. */
  supportsStandingOrderEndDate: boolean;
  /** This is only applicable to `MONTHLY` standing orders. Indicates whether the institution supports negative integers (-1 to -5) for setting up a `MONTHLY` standing order relative to the end of the month. */
  supportsStandingOrderNegativeExecutionDays: boolean;
  /** A list of the valid standing order intervals supported by the institution. */
  validStandingOrderIntervals: PaymentScheduleIntervalEnum[];
}

export const paymentInitiationStandingOrderMetadataSchema: Schema<PaymentInitiationStandingOrderMetadata> = object(
  {
    supportsStandingOrderEndDate: [
      'supports_standing_order_end_date',
      boolean(),
    ],
    supportsStandingOrderNegativeExecutionDays: [
      'supports_standing_order_negative_execution_days',
      boolean(),
    ],
    validStandingOrderIntervals: [
      'valid_standing_order_intervals',
      array(paymentScheduleIntervalEnumSchema),
    ],
  }
);
