/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, Schema, string } from '../schema.js';
import { BuyType, buyTypeSchema } from './buyType.js';
import { CashType, cashTypeSchema } from './cashType.js';
import { FeeType, feeTypeSchema } from './feeType.js';
import { SellType, sellTypeSchema } from './sellType.js';
import { TransferType, transferTypeSchema } from './transferType.js';

/** Valid values for investment transaction types and subtypes. Note that transactions representing inflow of cash will appear as negative amounts, outflow of cash will appear as positive amounts. */
export interface StandaloneInvestmentTransactionType {
  /** Buying an investment */
  buy: BuyType;
  /** Selling an investment */
  sell: SellType;
  /** A cancellation of a pending transaction */
  cancel: string;
  /** Activity that modifies a cash position */
  cash: CashType;
  /** Fees on the account, e.g. commission, bookkeeping, options-related. */
  fee: FeeType;
  /** Activity that modifies a position, but not through buy/sell activity e.g. options exercise, portfolio transfer */
  transfer: TransferType;
}

export const standaloneInvestmentTransactionTypeSchema: Schema<StandaloneInvestmentTransactionType> = lazy(
  () =>
    object({
      buy: ['buy', buyTypeSchema],
      sell: ['sell', sellTypeSchema],
      cancel: ['cancel', string()],
      cash: ['cash', cashTypeSchema],
      fee: ['fee', feeTypeSchema],
      transfer: ['transfer', transferTypeSchema],
    })
);
