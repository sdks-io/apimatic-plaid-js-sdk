/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** Defines the request schema for `/bank_transfer/migrate_account` */
export interface BankTransferMigrateAccountRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The user's account number. */
  accountNumber: string;
  /** The user's routing number. */
  routingNumber: string;
  /** The type of the bank account (`checking` or `savings`). */
  accountType: string;
  additionalProperties?: Record<string, unknown>;
}

export const bankTransferMigrateAccountRequestSchema: Schema<BankTransferMigrateAccountRequest> = typedExpandoObject(
  {
    clientId: ['client_id', optional(string())],
    secret: ['secret', optional(string())],
    accountNumber: ['account_number', string()],
    routingNumber: ['routing_number', string()],
    accountType: ['account_type', string()],
  },
  'additionalProperties',
  optional(unknown())
);
