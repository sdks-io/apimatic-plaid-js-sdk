/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** Account and bank identifier number data used to configure the test account. All values are optional. */
export interface Numbers {
  /** Will be used for the account number. */
  account?: string;
  /** Must be a valid ACH routing number. */
  achRouting?: string;
  /** Must be a valid wire transfer routing number. */
  achWireRouting?: string;
  /** EFT institution number. Must be specified alongside `eft_branch`. */
  eftInstitution?: string;
  /** EFT branch number. Must be specified alongside `eft_institution`. */
  eftBranch?: string;
  /** Bank identifier code (BIC). Must be specified alongside `international_iban`. */
  internationalBic?: string;
  /** International bank account number (IBAN). If no account number is specified via `account`, will also be used as the account number by default. Must be specified alongside `international_bic`. */
  internationalIban?: string;
  /** BACS sort code */
  bacsSortCode?: string;
  additionalProperties?: Record<string, unknown>;
}

export const numbersSchema: Schema<Numbers> = typedExpandoObject(
  {
    account: ['account', optional(string())],
    achRouting: ['ach_routing', optional(string())],
    achWireRouting: ['ach_wire_routing', optional(string())],
    eftInstitution: ['eft_institution', optional(string())],
    eftBranch: ['eft_branch', optional(string())],
    internationalBic: ['international_bic', optional(string())],
    internationalIban: ['international_iban', optional(string())],
    bacsSortCode: ['bacs_sort_code', optional(string())],
  },
  'additionalProperties',
  optional(unknown())
);
