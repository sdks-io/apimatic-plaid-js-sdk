/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { CountryCode1, countryCode1Schema } from './countryCode1.js';
import {
  DepositSwitchCreateRequestOptions,
  depositSwitchCreateRequestOptionsSchema,
} from './depositSwitchCreateRequestOptions.js';

/** DepositSwitchCreateRequest defines the request schema for `/deposit_switch/create` */
export interface DepositSwitchCreateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** Access token for the target Item, typically provided in the Import Item response. */
  targetAccessToken: string;
  /** Plaid Account ID that specifies the target bank account. This account will become the recipient for a user's direct deposit. */
  targetAccountId: string;
  /** ISO-3166-1 alpha-2 country code standard. */
  countryCode?: CountryCode1;
  /** Options to configure the `/deposit_switch/create` request. If provided, cannot be `null`. */
  options?: DepositSwitchCreateRequestOptions;
  additionalProperties?: Record<string, unknown>;
}

export const depositSwitchCreateRequestSchema: Schema<DepositSwitchCreateRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        targetAccessToken: ['target_access_token', string()],
        targetAccountId: ['target_account_id', string()],
        countryCode: ['country_code', optional(countryCode1Schema)],
        options: ['options', optional(depositSwitchCreateRequestOptionsSchema)],
      },
      'additionalProperties',
      optional(unknown())
    )
);
