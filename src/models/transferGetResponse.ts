/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, Schema, string } from '../schema.js';
import { Transfer, transferSchema } from './transfer.js';

/** Defines the response schema for `/transfer/get` */
export interface TransferGetResponse {
  /** Represents a transfer within the Transfers API. */
  transfer: Transfer;
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId: string;
}

export const transferGetResponseSchema: Schema<TransferGetResponse> = lazy(() =>
  object({
    transfer: ['transfer', transferSchema],
    requestId: ['request_id', string()],
  })
);
