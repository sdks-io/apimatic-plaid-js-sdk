/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  nullable,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  BankTransferDirection,
  bankTransferDirectionSchema,
} from './bankTransferDirection.js';
import {
  BankTransferEventType,
  bankTransferEventTypeSchema,
} from './bankTransferEventType.js';
import {
  BankTransferFailure,
  bankTransferFailureSchema,
} from './bankTransferFailure.js';
import {
  BankTransferReceiverDetails,
  bankTransferReceiverDetailsSchema,
} from './bankTransferReceiverDetails.js';
import {
  BankTransferType,
  bankTransferTypeSchema,
} from './bankTransferType.js';

/** Represents an event in the Bank Transfers API. */
export interface BankTransferEvent {
  /** Plaid’s unique identifier for this event. IDs are sequential unsigned 64-bit integers. */
  eventId: number;
  /** The datetime when this event occurred. This will be of the form `2006-01-02T15:04:05Z`. */
  timestamp: string;
  /**
   * The type of event that this bank transfer represents.
   * `pending`: A new transfer was created; it is in the pending state.
   * `cancelled`: The transfer was cancelled by the client.
   * `failed`: The transfer failed, no funds were moved.
   * `posted`: The transfer has been successfully submitted to the payment network.
   * `reversed`: A posted transfer was reversed.
   * `receiver_pending`: The matching transfer was found as a pending transaction in the receiver's account
   * `receiver_posted`: The matching transfer was found as a posted transaction in the receiver's account
   */
  eventType: BankTransferEventType;
  /** The account ID associated with the bank transfer. */
  accountId: string;
  /** Plaid’s unique identifier for a bank transfer. */
  bankTransferId: string;
  /** The ID of the origination account that this balance belongs to. */
  originationAccountId: string | null;
  /** The type of bank transfer. This will be either `debit` or `credit`.  A `debit` indicates a transfer of money into the origination account; a `credit` indicates a transfer of money out of the origination account. */
  bankTransferType: BankTransferType;
  /** The bank transfer amount. */
  bankTransferAmount: string;
  /** The currency of the bank transfer amount. */
  bankTransferIsoCurrencyCode: string;
  /** The failure reason if the type of this transfer is `"failed"` or `"reversed"`. Null value otherwise. */
  failureReason: BankTransferFailure;
  /** Indicates the direction of the transfer: `outbound` for API-initiated transfers, or `inbound` for payments received by the FBO account. */
  direction: BankTransferDirection;
  /** The receiver details if the type of this event is `reciever_pending` or `reciever_posted`. Null value otherwise. */
  receiverDetails: BankTransferReceiverDetails;
  additionalProperties?: Record<string, unknown>;
}

export const bankTransferEventSchema: Schema<BankTransferEvent> = lazy(() =>
  typedExpandoObject(
    {
      eventId: ['event_id', number()],
      timestamp: ['timestamp', string()],
      eventType: ['event_type', bankTransferEventTypeSchema],
      accountId: ['account_id', string()],
      bankTransferId: ['bank_transfer_id', string()],
      originationAccountId: ['origination_account_id', nullable(string())],
      bankTransferType: ['bank_transfer_type', bankTransferTypeSchema],
      bankTransferAmount: ['bank_transfer_amount', string()],
      bankTransferIsoCurrencyCode: [
        'bank_transfer_iso_currency_code',
        string(),
      ],
      failureReason: ['failure_reason', bankTransferFailureSchema],
      direction: ['direction', bankTransferDirectionSchema],
      receiverDetails: ['receiver_details', bankTransferReceiverDetailsSchema],
    },
    'additionalProperties',
    optional(unknown())
  )
);
