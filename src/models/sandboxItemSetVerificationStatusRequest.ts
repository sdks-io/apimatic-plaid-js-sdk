/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  VerificationStatus1,
  verificationStatus1Schema,
} from './verificationStatus1.js';

/** SandboxItemSetVerificationStatusRequest defines the request schema for `/sandbox/item/set_verification_status` */
export interface SandboxItemSetVerificationStatusRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The access token associated with the Item data is being requested for. */
  accessToken: string;
  /** The `account_id` of the account whose verification status is to be modified */
  accountId: string;
  /** The verification status to set the account to. */
  verificationStatus: VerificationStatus1;
  additionalProperties?: Record<string, unknown>;
}

export const sandboxItemSetVerificationStatusRequestSchema: Schema<SandboxItemSetVerificationStatusRequest> = typedExpandoObject(
  {
    clientId: ['client_id', optional(string())],
    secret: ['secret', optional(string())],
    accessToken: ['access_token', string()],
    accountId: ['account_id', string()],
    verificationStatus: ['verification_status', verificationStatus1Schema],
  },
  'additionalProperties',
  optional(unknown())
);
