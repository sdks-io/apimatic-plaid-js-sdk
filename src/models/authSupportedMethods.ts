/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** Metadata specifically related to which auth methods an institution supports. */
export interface AuthSupportedMethods {
  /** Indicates if instant auth is supported. */
  instantAuth: boolean;
  /** Indicates if instant match is supported. */
  instantMatch: boolean;
  /** Indicates if automated microdeposits are supported. */
  automatedMicroDeposits: boolean;
  additionalProperties?: Record<string, unknown>;
}

export const authSupportedMethodsSchema: Schema<AuthSupportedMethods> = typedExpandoObject(
  {
    instantAuth: ['instant_auth', boolean()],
    instantMatch: ['instant_match', boolean()],
    automatedMicroDeposits: ['automated_micro_deposits', boolean()],
  },
  'additionalProperties',
  optional(unknown())
);
