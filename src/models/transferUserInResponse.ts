/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, nullable, object, Schema, string } from '../schema.js';
import {
  TransferUserAddressInResponse,
  transferUserAddressInResponseSchema,
} from './transferUserAddressInResponse.js';

/** The legal name and other information for the account holder. */
export interface TransferUserInResponse {
  /** The user's legal name. */
  legalName: string;
  /** The user's phone number. */
  phoneNumber: string | null;
  /** The user's email address. */
  emailAddress: string | null;
  /** The address associated with the account holder. */
  address: TransferUserAddressInResponse;
}

export const transferUserInResponseSchema: Schema<TransferUserInResponse> = lazy(
  () =>
    object({
      legalName: ['legal_name', string()],
      phoneNumber: ['phone_number', nullable(string())],
      emailAddress: ['email_address', nullable(string())],
      address: ['address', transferUserAddressInResponseSchema],
    })
);
