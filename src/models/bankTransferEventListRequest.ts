/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  nullable,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  BankTransferDirection1,
  bankTransferDirection1Schema,
} from './bankTransferDirection1.js';
import {
  BankTransferEventType,
  bankTransferEventTypeSchema,
} from './bankTransferEventType.js';
import {
  BankTransferType1,
  bankTransferType1Schema,
} from './bankTransferType1.js';

/** Defines the request schema for `/bank_transfer/event/list` */
export interface BankTransferEventListRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The start datetime of bank transfers to list. This should be in RFC 3339 format (i.e. `2019-12-06T22:35:49Z`) */
  startDate?: string | null;
  /** The end datetime of bank transfers to list. This should be in RFC 3339 format (i.e. `2019-12-06T22:35:49Z`) */
  endDate?: string | null;
  /** Plaid’s unique identifier for a bank transfer. */
  bankTransferId?: string | null;
  /** The account ID to get events for all transactions to/from an account. */
  accountId?: string | null;
  /** The type of bank transfer. This will be either `debit` or `credit`.  A `debit` indicates a transfer of money into your origination account; a `credit` indicates a transfer of money out of your origination account. */
  bankTransferType?: BankTransferType1;
  /** Filter events by event type. */
  eventTypes?: BankTransferEventType[];
  /** The maximum number of bank transfer events to return. If the number of events matching the above parameters is greater than `count`, the most recent events will be returned. */
  count?: number | null;
  /** The offset into the list of bank transfer events. When `count`=25 and `offset`=0, the first 25 events will be returned. When `count`=25 and `offset`=25, the next 25 bank transfer events will be returned. */
  offset?: number | null;
  /** The origination account ID to get events for transfers from a specific origination account. */
  originationAccountId?: string | null;
  /**
   * Indicates the direction of the transfer: `outbound`: for API-initiated transfers
   * `inbound`: for payments received by the FBO account.
   */
  direction?: BankTransferDirection1;
  additionalProperties?: Record<string, unknown>;
}

export const bankTransferEventListRequestSchema: Schema<BankTransferEventListRequest> = typedExpandoObject(
  {
    clientId: ['client_id', optional(string())],
    secret: ['secret', optional(string())],
    startDate: ['start_date', optional(nullable(string()))],
    endDate: ['end_date', optional(nullable(string()))],
    bankTransferId: ['bank_transfer_id', optional(nullable(string()))],
    accountId: ['account_id', optional(nullable(string()))],
    bankTransferType: ['bank_transfer_type', optional(bankTransferType1Schema)],
    eventTypes: ['event_types', optional(array(bankTransferEventTypeSchema))],
    count: ['count', optional(nullable(number()))],
    offset: ['offset', optional(nullable(number()))],
    originationAccountId: [
      'origination_account_id',
      optional(nullable(string())),
    ],
    direction: ['direction', optional(bankTransferDirection1Schema)],
  },
  'additionalProperties',
  optional(unknown())
);
