/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { array, lazy, object, optional, Schema, string } from '../schema.js';
import {
  ConnectedApplication,
  connectedApplicationSchema,
} from './connectedApplication.js';

/** Describes the connected application for a particular end user. */
export interface ItemApplicationListResponse {
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId?: string;
  /** A list of connected applications. */
  applications: ConnectedApplication[];
}

export const itemApplicationListResponseSchema: Schema<ItemApplicationListResponse> = lazy(
  () =>
    object({
      requestId: ['request_id', optional(string())],
      applications: ['applications', array(connectedApplicationSchema)],
    })
);
