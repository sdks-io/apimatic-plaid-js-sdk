/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  IncomeVerificationPrecheckEmployer,
  incomeVerificationPrecheckEmployerSchema,
} from './incomeVerificationPrecheckEmployer.js';
import {
  IncomeVerificationPrecheckMilitaryInfo,
  incomeVerificationPrecheckMilitaryInfoSchema,
} from './incomeVerificationPrecheckMilitaryInfo.js';
import {
  IncomeVerificationPrecheckUser,
  incomeVerificationPrecheckUserSchema,
} from './incomeVerificationPrecheckUser.js';

/** IncomeVerificationPrecheckRequest defines the request schema for `/income/verification/precheck` */
export interface IncomeVerificationPrecheckRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  user?: IncomeVerificationPrecheckUser;
  employer?: IncomeVerificationPrecheckEmployer;
  /** The access token associated with the Item data is being requested for. */
  transactionsAccessToken?: string | null;
  usMilitaryInfo?: IncomeVerificationPrecheckMilitaryInfo;
  additionalProperties?: Record<string, unknown>;
}

export const incomeVerificationPrecheckRequestSchema: Schema<IncomeVerificationPrecheckRequest> = lazy(
  () =>
    typedExpandoObject(
      {
        clientId: ['client_id', optional(string())],
        secret: ['secret', optional(string())],
        user: ['user', optional(incomeVerificationPrecheckUserSchema)],
        employer: [
          'employer',
          optional(incomeVerificationPrecheckEmployerSchema),
        ],
        transactionsAccessToken: [
          'transactions_access_token',
          optional(nullable(string())),
        ],
        usMilitaryInfo: [
          'us_military_info',
          optional(incomeVerificationPrecheckMilitaryInfoSchema),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
