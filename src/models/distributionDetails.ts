/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, nullable, object, optional, Schema, string } from '../schema.js';
import { Pay, paySchema } from './pay.js';

/** An object representing information about a distribution from the paycheck (for example, the amount distributed to a specific checking account, or to a retirement plan). */
export interface DistributionDetails {
  /** The account number of the account being deposited to. */
  accountNumber?: string | null;
  /** The type of bank account (e.g. Checking or Savings) */
  bankAccountType?: string | null;
  /** The name of the bank that the payment is being deposited to. */
  bankName?: string | null;
  /** An object representing a monetary amount. */
  currentPay?: Pay;
  /** A description of the distribution type. */
  description?: string | null;
}

export const distributionDetailsSchema: Schema<DistributionDetails> = lazy(() =>
  object({
    accountNumber: ['account_number', optional(nullable(string()))],
    bankAccountType: ['bank_account_type', optional(nullable(string()))],
    bankName: ['bank_name', optional(nullable(string()))],
    currentPay: ['current_pay', optional(paySchema)],
    description: ['description', optional(nullable(string()))],
  })
);
