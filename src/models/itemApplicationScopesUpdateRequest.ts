/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, optional, Schema, string } from '../schema.js';
import { Scopes, scopesSchema } from './scopes.js';
import {
  ScopesContextEnum,
  scopesContextEnumSchema,
} from './scopesContextEnum.js';

/** ItemApplicationScopesUpdateRequest defines the request schema for `/item/application/scopes/update` */
export interface ItemApplicationScopesUpdateRequest {
  /** Your Plaid API `client_id`. The `client_id` is required and may be provided either in the `PLAID-CLIENT-ID` header or as part of a request body. */
  clientId?: string;
  /** Your Plaid API `secret`. The `secret` is required and may be provided either in the `PLAID-SECRET` header or as part of a request body. */
  secret?: string;
  /** The access token associated with the Item data is being requested for. */
  accessToken: string;
  /** This field will map to the application ID that is returned from /item/applications/list, or provided to the institution in an oauth redirect. */
  applicationId: string;
  /** The scopes object */
  scopes: Scopes;
  /** When scopes are updated during enrollment, this field must be populated with the state sent to the partner in the OAuth Login URI. This field is required when the context is `ENROLLMENT`. */
  state?: string;
  /** An indicator for when scopes are being updated. When scopes are updated via enrollment (i.e. OAuth), the partner must send `ENROLLMENT`. When scopes are updated in a post-enrollment view, the partner must send `PORTAL`. */
  context: ScopesContextEnum;
}

export const itemApplicationScopesUpdateRequestSchema: Schema<ItemApplicationScopesUpdateRequest> = lazy(
  () =>
    object({
      clientId: ['client_id', optional(string())],
      secret: ['secret', optional(string())],
      accessToken: ['access_token', string()],
      applicationId: ['application_id', string()],
      scopes: ['scopes', scopesSchema],
      state: ['state', optional(string())],
      context: ['context', scopesContextEnumSchema],
    })
);
