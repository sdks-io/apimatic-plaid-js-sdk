/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  lazy,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  InstitutionsSearchPaymentInitiationOptions,
  institutionsSearchPaymentInitiationOptionsSchema,
} from './institutionsSearchPaymentInitiationOptions.js';

/** An optional object to filter `/institutions/search` results. */
export interface InstitutionsSearchRequestOptions {
  /** Limit results to institutions with or without OAuth login flows. This is primarily relevant to institutions with European country codes */
  oauth?: boolean;
  /** When true, return the institution's homepage URL, logo and primary brand color. */
  includeOptionalMetadata?: boolean;
  /** When `true`, returns metadata related to the Auth product indicating which auth methods are supported. */
  includeAuthMetadata?: boolean;
  /** When `true`, returns metadata related to the Payment Initiation product indicating which payment configurations are supported. */
  includePaymentInitiationMetadata?: boolean;
  /** Additional options that will be used to filter institutions by various Payment Initiation configurations. */
  paymentInitiation?: InstitutionsSearchPaymentInitiationOptions;
  additionalProperties?: Record<string, unknown>;
}

export const institutionsSearchRequestOptionsSchema: Schema<InstitutionsSearchRequestOptions> = lazy(
  () =>
    typedExpandoObject(
      {
        oauth: ['oauth', optional(boolean())],
        includeOptionalMetadata: [
          'include_optional_metadata',
          optional(boolean()),
        ],
        includeAuthMetadata: ['include_auth_metadata', optional(boolean())],
        includePaymentInitiationMetadata: [
          'include_payment_initiation_metadata',
          optional(boolean()),
        ],
        paymentInitiation: [
          'payment_initiation',
          optional(institutionsSearchPaymentInitiationOptionsSchema),
        ],
      },
      'additionalProperties',
      optional(unknown())
    )
);
