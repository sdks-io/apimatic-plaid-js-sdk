/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import {
  CanonicalDescription,
  canonicalDescriptionSchema,
} from './canonicalDescription.js';
import { Pay, paySchema } from './pay.js';

/** An object representing both the current pay period and year to date amount for a category. */
export interface Total {
  /** Commonly used term to describe the line item. */
  canonicalDescription?: CanonicalDescription;
  /** Text of the line item as printed on the paystub. */
  description?: string | null;
  /** An object representing a monetary amount. */
  currentPay?: Pay;
  /** An object representing a monetary amount. */
  ytdPay?: Pay;
  additionalProperties?: Record<string, unknown>;
}

export const totalSchema: Schema<Total> = lazy(() =>
  typedExpandoObject(
    {
      canonicalDescription: [
        'canonical_description',
        optional(canonicalDescriptionSchema),
      ],
      description: ['description', optional(nullable(string()))],
      currentPay: ['current_pay', optional(paySchema)],
      ytdPay: ['ytd_pay', optional(paySchema)],
    },
    'additionalProperties',
    optional(unknown())
  )
);
