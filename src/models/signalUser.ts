/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, nullable, object, optional, Schema, string } from '../schema.js';
import { AddressData1, addressData1Schema } from './addressData1.js';
import {
  SignalPersonName,
  signalPersonNameSchema,
} from './signalPersonName.js';

/** Details about the end user initiating the transaction (i.e., the account holder). */
export interface SignalUser {
  /** The user's legal name */
  name?: SignalPersonName;
  /** The user's phone number, in E.164 format: +{countrycode}{number}. For example: "+14151234567" */
  phoneNumber?: string | null;
  /** The user's email address. */
  emailAddress?: string | null;
  /** Data about the components comprising an address. */
  address?: AddressData1;
}

export const signalUserSchema: Schema<SignalUser> = lazy(() =>
  object({
    name: ['name', optional(signalPersonNameSchema)],
    phoneNumber: ['phone_number', optional(nullable(string()))],
    emailAddress: ['email_address', optional(nullable(string()))],
    address: ['address', optional(addressData1Schema)],
  })
);
