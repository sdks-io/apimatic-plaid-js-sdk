/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  boolean,
  lazy,
  nullable,
  object,
  optional,
  Schema,
} from '../schema.js';
import { AccountAccess, accountAccessSchema } from './accountAccess.js';
import { ProductAccess, productAccessSchema } from './productAccess.js';

export interface ScopesNullable {
  /** The product access being requested. Used to or disallow product access across all accounts. If unset, defaults to all products allowed. */
  productAccess?: ProductAccess;
  accounts?: AccountAccess[];
  /** Allow access to newly opened accounts as they are opened. If unset, defaults to `true`. */
  newAccounts?: boolean | null;
}

export const scopesNullableSchema: Schema<ScopesNullable> = lazy(() =>
  object({
    productAccess: ['product_access', optional(productAccessSchema)],
    accounts: ['accounts', optional(array(accountAccessSchema))],
    newAccounts: ['new_accounts', optional(nullable(boolean()))],
  })
);
