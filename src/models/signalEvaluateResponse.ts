/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, object, Schema, string } from '../schema.js';
import {
  SignalEvaluateCoreAttributes,
  signalEvaluateCoreAttributesSchema,
} from './signalEvaluateCoreAttributes.js';
import {
  SignalEvaluateScores,
  signalEvaluateScoresSchema,
} from './signalEvaluateScores.js';

/** SignalEvaluateResponse defines the response schema for `/signal/income/evaluate` */
export interface SignalEvaluateResponse {
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId: string;
  /** Risk scoring details broken down by risk category. */
  scores: SignalEvaluateScores;
  /** The core attributes object contains additional data that can be used to assess the ACH return risk, such as past ACH return events, balance/transaction history, the Item’s connection history in the Plaid network, and identity change history. */
  coreAttributes: SignalEvaluateCoreAttributes;
}

export const signalEvaluateResponseSchema: Schema<SignalEvaluateResponse> = lazy(
  () =>
    object({
      requestId: ['request_id', string()],
      scores: ['scores', signalEvaluateScoresSchema],
      coreAttributes: ['core_attributes', signalEvaluateCoreAttributesSchema],
    })
);
