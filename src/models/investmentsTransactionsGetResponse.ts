/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  number,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Account, accountSchema } from './account.js';
import {
  InvestmentTransaction,
  investmentTransactionSchema,
} from './investmentTransaction.js';
import { Item, itemSchema } from './item.js';
import { Security, securitySchema } from './security.js';

/** InvestmentsTransactionsGetResponse defines the response schema for `/investments/transactions/get` */
export interface InvestmentsTransactionsGetResponse {
  /** Metadata about the Item. */
  item: Item;
  /** The accounts for which transaction history is being fetched. */
  accounts: Account[];
  /** All securities for which there is a corresponding transaction being fetched. */
  securities: Security[];
  /** The transactions being fetched */
  investmentTransactions: InvestmentTransaction[];
  /** The total number of transactions available within the date range specified. If `total_investment_transactions` is larger than the size of the `transactions` array, more transactions are available and can be fetched via manipulating the `offset` parameter.' */
  totalInvestmentTransactions: number;
  /** A unique identifier for the request, which can be used for troubleshooting. This identifier, like all Plaid identifiers, is case sensitive. */
  requestId: string;
  additionalProperties?: Record<string, unknown>;
}

export const investmentsTransactionsGetResponseSchema: Schema<InvestmentsTransactionsGetResponse> = lazy(
  () =>
    typedExpandoObject(
      {
        item: ['item', itemSchema],
        accounts: ['accounts', array(accountSchema)],
        securities: ['securities', array(securitySchema)],
        investmentTransactions: [
          'investment_transactions',
          array(investmentTransactionSchema),
        ],
        totalInvestmentTransactions: [
          'total_investment_transactions',
          number(),
        ],
        requestId: ['request_id', string()],
      },
      'additionalProperties',
      optional(unknown())
    )
);
