/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  lazy,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { Status, statusSchema } from './status.js';
import { StatusBreakdown, statusBreakdownSchema } from './statusBreakdown.js';

/** A representation of the status health of a request type. Auth requests, Balance requests, Identity requests, Investments requests, Liabilities requests, Transactions updates, Investments updates, Liabilities updates, and Item logins each have their own status object. */
export interface ProductStatus {
  /**
   * `HEALTHY`: the majority of requests are successful
   * `DEGRADED`: only some requests are successful
   * `DOWN`: all requests are failing
   */
  status: Status;
  /** [ISO 8601](https://wikipedia.org/wiki/ISO_8601) formatted timestamp of the last status change for the institution. */
  lastStatusChange: string;
  /** A detailed breakdown of the institution's performance for a request type. The values for `success`, `error_plaid`, and `error_institution` sum to 1. */
  breakdown: StatusBreakdown;
  additionalProperties?: Record<string, unknown>;
}

export const productStatusSchema: Schema<ProductStatus> = lazy(() =>
  typedExpandoObject(
    {
      status: ['status', statusSchema],
      lastStatusChange: ['last_status_change', string()],
      breakdown: ['breakdown', statusBreakdownSchema],
    },
    'additionalProperties',
    optional(unknown())
  )
);
