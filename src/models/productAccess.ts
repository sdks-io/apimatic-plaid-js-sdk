/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  boolean,
  nullable,
  optional,
  Schema,
  typedExpandoObject,
  unknown,
} from '../schema.js';

/** The product access being requested. Used to or disallow product access across all accounts. If unset, defaults to all products allowed. */
export interface ProductAccess {
  /** Allow access to statements. If unset, defaults to `true`. */
  statements?: boolean | null;
  /** Allow access to the Identity product (name, email, phone, address). If unset, defaults to `true`. */
  identity?: boolean | null;
  /** Allow access to account number details. If unset, defaults to `true`. */
  auth?: boolean | null;
  /** Allow access to transaction details. If unset, defaults to `true`. */
  transactions?: boolean | null;
  additionalProperties?: Record<string, unknown>;
}

export const productAccessSchema: Schema<ProductAccess> = typedExpandoObject(
  {
    statements: ['statements', optional(nullable(boolean()))],
    identity: ['identity', optional(nullable(boolean()))],
    auth: ['auth', optional(nullable(boolean()))],
    transactions: ['transactions', optional(nullable(boolean()))],
  },
  'additionalProperties',
  optional(unknown())
);
