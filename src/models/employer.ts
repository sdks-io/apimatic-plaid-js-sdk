/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import { lazy, number, object, Schema, string } from '../schema.js';
import {
  AddressDataNullable,
  addressDataNullableSchema,
} from './addressDataNullable.js';

/** Data about the employer. */
export interface Employer {
  /** Plaid's unique identifier for the employer. */
  employerId: string;
  /** The name of the employer */
  name: string;
  address: AddressDataNullable;
  /** A number from 0 to 1 indicating Plaid's level of confidence in the pairing between the employer and the institution (not yet implemented). */
  confidenceScore: number;
}

export const employerSchema: Schema<Employer> = lazy(() =>
  object({
    employerId: ['employer_id', string()],
    name: ['name', string()],
    address: ['address', addressDataNullableSchema],
    confidenceScore: ['confidence_score', number()],
  })
);
