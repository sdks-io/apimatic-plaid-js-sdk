/**
 * The Plaid APILib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  array,
  lazy,
  nullable,
  object,
  optional,
  Schema,
  string,
} from '../schema.js';
import {
  ProductDataTypeEnum,
  productDataTypeEnumSchema,
} from './productDataTypeEnum.js';
import { RequestedScopes, requestedScopesSchema } from './requestedScopes.js';
import { ScopesNullable, scopesNullableSchema } from './scopesNullable.js';

/** Describes the connected application for a particular end user. */
export interface ConnectedApplication {
  /** This field will map to the application ID that is returned from /item/applications/list, or provided to the institution in an oauth redirect. */
  applicationId: string;
  /** The name of the application */
  name: string;
  /** A URL that links to the application logo image (will be deprecated in the future, please use logo_url). */
  logo: string | null;
  /** A URL that links to the application logo image. */
  logoUrl: string | null;
  /** The URL for the application's website */
  applicationUrl: string | null;
  /** A string provided by the connected app stating why they use their respective enabled products. */
  reasonForAccess: string | null;
  /** The date this application was linked in [ISO 8601](https://wikipedia.org/wiki/ISO_8601) (YYYY-MM-DD) format in UTC. */
  createdAt: string;
  /** (Deprecated) A list of enums representing the data collected and products enabled for this connected application. */
  productDataTypes: ProductDataTypeEnum[];
  scopes?: ScopesNullable;
  /** Scope of required and optional account features or content from a ConnectedApplication. */
  requestedScopes?: RequestedScopes;
}

export const connectedApplicationSchema: Schema<ConnectedApplication> = lazy(
  () =>
    object({
      applicationId: ['application_id', string()],
      name: ['name', string()],
      logo: ['logo', nullable(string())],
      logoUrl: ['logo_url', nullable(string())],
      applicationUrl: ['application_url', nullable(string())],
      reasonForAccess: ['reason_for_access', nullable(string())],
      createdAt: ['created_at', string()],
      productDataTypes: [
        'product_data_types',
        array(productDataTypeEnumSchema),
      ],
      scopes: ['scopes', optional(scopesNullableSchema)],
      requestedScopes: ['requested_scopes', optional(requestedScopesSchema)],
    })
);
