/**
 * Plaid
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

import {
  nullable,
  optional,
  Schema,
  string,
  typedExpandoObject,
  unknown,
} from '../schema.js';
import { PayFrequency1, payFrequency1Schema } from './payFrequency1.js';

/** An object representing details that can be found on the paystub. */
export interface PaystubDetails {
  /** Beginning date of the pay period on the paystub in the 'YYYY-MM-DD' format. */
  payPeriodStartDate?: string | null;
  /** Ending date of the pay period on the paystub in the 'YYYY-MM-DD' format. */
  payPeriodEndDate?: string | null;
  /** Pay date on the paystub in the 'YYYY-MM-DD' format. */
  payDate?: string | null;
  /** The name of the payroll provider that generated the paystub, e.g. ADP */
  paystubProvider?: string | null;
  /** The frequency at which the employee is paid. Possible values: `MONTHLY`, `BI-WEEKLY`, `WEEKLY`, `SEMI-MONTHLY`. */
  payFrequency?: PayFrequency1;
  additionalProperties?: Record<string, unknown>;
}

export const paystubDetailsSchema: Schema<PaystubDetails> = typedExpandoObject(
  {
    payPeriodStartDate: ['pay_period_start_date', optional(nullable(string()))],
    payPeriodEndDate: ['pay_period_end_date', optional(nullable(string()))],
    payDate: ['pay_date', optional(nullable(string()))],
    paystubProvider: ['paystub_provider', optional(nullable(string()))],
    payFrequency: ['pay_frequency', optional(payFrequency1Schema)],
  },
  'additionalProperties',
  optional(unknown())
);
